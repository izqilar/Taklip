# 2026-09-25 工作日志

## 服务商入驻与归属管线改造（依据 docs/服务商入驻与归属管线改造方案.md）

用户审阅方案后授权实施。已完成 P0/P1 两个 commit：

### commit 13f353b —— M1/M2（阶段一：安全 + 归属基线）

**M1 关闭自服务入驻捷径（安全缺口 P-A）**
- `auth.service.applyForProvider`：不再直接改 role，改为仅创建
  `QualificationApplication(status=FIRST_PENDING)`；已是已审服务商转走扩展业务管线；在途申请幂等复用。
- 移除 `auth.service.submitProviderReview`（自助置 `providerStatus=APPROVED`）与
  `POST /api/auth/submit-provider-review` 公开端点。
- web `api/client.ts`、`store/authStore.ts` 同步语义（申请不再即时升级本地身份）。
- 关键事实：`.tsx` 中**无** applyForProvider/submitProviderReview 调用，前端改造风险低；
  正规入驻入口 `apps/web/src/user/Apply.tsx:237` 本就走 `POST /api/user/qualifications`。

**M2 归属判定落地（P-C/P-D/P-F）**
- `user-console.applyQualificationResult` provider 分支：固化 `regionPath`，
  并按**最长前缀匹配**写 `User.agentId`；钱包初始化随入驻真源收敛迁移至此。
- 新增 `resolveAgentId`（最长前缀）与 `findRegionConflict`（双向辖区重叠检测）；
  代理商终审**前置**拦截辖区重叠（须在写申请状态之前，否则会出现"申请已过审但身份未落地"不一致）。
- **未加 DB 唯一约束**：`regionPath` 需被同辖区多服务商共用，`@@unique([role,regionPath])`
  会误伤（两个同区服务商冲突），故改为应用层拦截。
- 新增 `prisma/backfill-provider-agent.js`（幂等，支持 `--dry`）：
  实跑 5 位代理商辖区无重叠、15 位服务商全部归属、0 跳过。

### commit cbcb364 —— M3/M4（阶段二：流程 + 合同）

**M3 材料前置（P-E）**
- `POST /api/user/qualifications` 支持申请时即带材料；
  `fillQualification` 状态约束放宽为 `FIRST_PENDING|FIRST_PASSED`：
  一审前提交维持 FIRST_PENDING（第一闸口审真材料），一审后补填仍进 FINAL_PENDING（兼容）。

**M4 合同签约真实阶段（P-B）**
- 终审 APPROVED 后系统自动生成主合同草稿（`ensureProviderContract`，幂等），
  `contractNo=HT-YYYY-SPXXX`，甲方恒为平台主体（**代理商非签约方**）。
- 新增 `POST /api/provider/contracts/:id/advance`（仅 ADMIN）：
  `AWAIT_SENIOR_SIGN → APPROVING → EFFECTIVE`。原 `signContract` 只能推进到
  AWAIT_SENIOR_SIGN，合同永远无法生效。
- 合同即付费闸门：`template.service` 与 `provider-console.createService` 均校验
  "存在 MAIN 合同时须 EFFECTIVE"。
  **坑**：`createService`（provider-console:396）直接 `prisma.template.create`，
  **绕过了** template.service 的付费校验 —— 闸门必须两处都加才生效（首次冒烟在此失败）。
- 兼容处理：主合同是新增产物，存量服务商可能未持有，故仅"存在 MAIN 合同时"强制，
  避免一刀切误伤存量业务。

## 验证
- server + web typecheck 全绿；`build:compiled` 通过并重启生效。
- 端到端冒烟：M1/M2 **13/13**、M3/M4 **12/12** 全通过（临时脚本跑完即删）。
- 测试账号：ADMIN 13800000002/dev123456；AGENT phone 13900003001/Test123456（辖区 65/6501）；
  USER 13900001001/Test123456。

## 环境/运维要点
- 改 server 源码后：`build:compiled` → kill :3000（watchdog 会拉起新 dist）。
  手动 `node dist/main` 常遇 EADDRINUSE（watchdog 已占端口），属正常，直接复用即可。
- `git push` 失败：`Failed to connect to github.com:443`（沙箱网络不可达），
  本地 commit 已就绪，凭据已存 `~/.git-credentials`，网络恢复后 `git push origin main` 即可。

## 待办（P2，未实施）
- M5 总台自动化风险预检 + 分层人工审（自动审批风险较高，建议先只做预检标记、不自动放行）。
- M6 agent 审核质量护栏（KPI/逆向审计统计端点）。
- M7 前端配套：Apply.tsx 材料表单前置、运营端合同页加"推进签署"动作、
  `GET /api/agent/qualifications` 加 `kind:'provider'` 过滤（P-G，避免混入 agent 类申请）。

## 新客户注册为代理商/服务商方案（设计稿，未实施）
产出 `docs/新客户注册为代理商或服务商方案.md`。**推荐方案：扩展现有注册流程（注册页加账户类型），
不新建独立入驻通道**（防再次出现 M1 那种绕过审批的旁路；手机号 `@unique` 天然防重复建户）。
核心设计：两段式提交 —— `POST /api/auth/register` 只扩轻量可选字段（intent/realName/regionId，
**不承载证件附件、不写 role**），注册后持 token 走既有受保护接口 `/api/user/qualifications` 提交材料。

核实到的新缺口（本轮发现，方案需一并解决）：
- 总台 `POST /api/user/qualifications/:id/review`（user-console:1352，@Roles ADMIN）body 仅
  `{pass, final}`，**不接 note**，且 `QualificationApplication` 无 `reviewNote` 字段 → 驳回原因无处落库。
- 入驻管线**无任何结果通知**（JOIN 隧道有 `staff.service.notifyJoinResult` 写 Message scope=USER，
  入驻没有）；"初审通过"只是 user-console:899 的通知聚合，非审核回执。
- `DELETE /api/user/qualifications/:id`（user-console:1326）是**物理 delete**，审计留痕丢失，
  与 JOIN 隧道 `WITHDRAWN` 软删除语义不一致。
- `certNo` 无唯一约束 → 换手机号即可重复入驻（手机号唯一挡不住）。
- 入驻入口 `apps/web/src/user/Apply.tsx` 挂 `ProtectedRoute`（App.tsx:85~105）下，
  新客户必须先注册登录才能申请 —— 注册页无账户类型（Register.tsx 仅手机/密码/昵称）。
- 仓库 `apps/` 只有 admin/server/web，**无独立 APP 工程**；用户口中的"APP"= web H5。

### 已实施（P0/P1，两个 commit）

**commit 587a3b8 —— P0 堵缺口**
- schema `QualificationApplication` 新增 `reviewNote`/`firstReviewedById|At`/`finalReviewedById|At`/
  `materialSubmittedAt`/`resubmitOfId`/`riskFlags`，补 `@@index([kind,status])`、`@@index([certNo])`。
- 提交侧强校验：代理商辖区**必须三级（省/市/区）**（否则一个代理商可罩全省）；
  **辖区重叠预检前移到提交时**（原来只在终审前拦，用户返工太晚）；材料"填了必须填全"。
- 风险旗标：`MATERIAL_MISSING` / `DUPLICATE_CERT_NO` / `NO_REGION` / `NO_AGENT_COVERAGE`。
- 驳回（一审+终审）**原因必填**；总台 review 新增 `note` 入参 + 落库 + `auditLog`（此前终审零审计）。
- 新增 `notifyQualificationResult`：四类回执走 `Message(scope=USER)`（对齐 `staff.service.notifyJoinResult`）。
- 撤回改 **WITHDRAWN 软删除**（原物理 delete 丢审计）；代理商列表加 `kind` 过滤且默认隐藏已撤回。
- 冒烟 35/35。

**commit f501350 —— P1 注册即入驻入口**
- `RegisterDto`+`register()` 扩可选 `intent/realName/regionId`（写 regionPath），**不写 role**；
  无鉴权接口不接证件附件（防刷）。
- 新增 `apps/web/src/pages/Onboarding.tsx`（`/onboarding`，ProtectedRoute）+ `Register.tsx` 账户类型三选一。
- i18n：zh-CN/en 补 `errors.register.*`、`common.onboarding.*`。
- 冒烟 10/10（注册后仍 USER、资质仍 PENDING、重复提交识别、辖区无覆盖旗标、代理商未到区县被拦）。

### 本次环境坑
- `prisma generate` 报 EPERM 重命名 dll：**运行中的 server 锁住了 dll**。先 kill :3000 再立刻 generate
  （watchdog 5s 会拉起，动作要快）。
- 冒烟断言注意：Nest **POST 默认返回 201**（不是 200）；`DELETE /api/user/qualifications/:id`
  只允许 FIRST_PENDING/FIRST_PASSED 撤回，已驳回单本就不可撤（不是 bug）。

### P2 收尾（同日续做，已全部落地）

**commit a6e4d8b —— 材料表单 + 强校验 + certNo 唯一性**
- `Apply.tsx` 补齐「主体资质材料」区块（姓名/手机/证件类型/证件号/有效期+长期/发证机关/附件），
  与 `/onboarding` 同口径；实名与手机号预填自当前用户。
- `MATERIALS_REQUIRED` 置 **true**；服务商 `regionPath` 由选填改**必填**（消灭无代理商管辖的孤立主体）。
- certNo **分级**唯一性：命中已入驻主体（`APPROVED`）→ 拦截；仅命中在途 → 只打 `DUPLICATE_CERT_NO` 旗标
  （防历史脏数据误伤）。冒烟 11/11。

**commit 1af83a9 —— 详情可见化 + 实名联动 + 代理协议 + 合同推进 + M5/M6**
- schema：`QualificationApplication.notifiedAt`（通知留痕）。
- 终审 `APPROVED` 同步置 `realNameStatus=APPROVED` + 固化实名（决策 3）；
  代理商终审自动生成 `type=AGENCY` 代理协议（合同号 HT-YYYY-AGxxx，甲方平台、区域独家）。
- 总台审批页：kind/状态筛选、风险旗标与「已通知」列、详情抽屉（材料+附件+审核意见+审核人/时间+
  历史被驳回数+预检结果）、**终审驳回必填原因**；代理商页默认只看 provider 并补详情字段。
- 合同详情页对 ADMIN 暴露「推进至审批/确认生效」——**advance 端点必须传 body.stage**，
  缺省会直接跳到 EFFECTIVE（踩过）。
- 新增 `GET /api/admin/qualifications/:id/precheck`（M5：**只标记不自动放行**）与
  `GET /api/admin/agent-review-quality`（M6：一审量/驳回/被终审推翻率/平均时长）。
- badges 新增 `onboardingFirst`（代理商待一审）/ `onboardingFinal`（总台待终审），`onboarding` 保留合计。
- 冒烟 22/22。

**commit 086dd41 —— 重提溯源 + 招商转化**
- `resubmitOfId` 接线：仅本人 REJECTED/WITHDRAWN 可重提且层次须一致，生成新申请、旧单留历史；
  `Apply.tsx`「重新提交」预填材料与区域。
- 招商意向转入驻：**不代客创建申请**（材料已强制必填 + 避免代理商既招揽又自审），
  只置 WON + 审计 + 返回入驻邀请链接（`WEB_ORIGIN` 可配，缺省 5173）。冒烟 11/11。

**commit edeef78** —— 方案文档补 §9 实施记录与 §7 九项决策、§9.3 刻意未做项（自动审批 /
regionPath DB 硬约束 / 可靠电签 / 短信通道）。

### 本次新增环境坑
- **python 写文件会把 LF 改成 CRLF**，导致整个文件进 diff（Apply.tsx 一次 +739/-739）。
  用 python 改写仓库文件后必须 `b.replace(b'\r\n', b'\n')` 二进制写回，或直接用 Edit 工具。
- 总台入驻列表端点在 `apps/server/src/admin/admin.controller.ts:118`（`@Controller('api/admin')` 有
  **两个**文件：admin.controller.ts 与 agent/agent.controller.ts），别找错。

### 剩余
- `git push` 仍失败：网络不可达 github.com:443（凭据已存 ~/.git-credentials，恢复后 `git push origin main`）。
  本地 commit 已到 edeef78。

### 安全与正确性审查（注册/入驻/归属改造）
- 产出报告：`docs/安全与正确性审查-注册与入驻改造.md`。
- 结论：无 Critical/可提权漏洞。中危 M1/M2（注册/apply-provider 自断言权威 regionPath，污染访问控制与代理商归属——当前 web 不传 regionId 未触发，需纵深收敛）；M3（JWT 默认回退密钥，投产前必改）；M4（限流内存 Map+未 trust-proxy，集群/反代下失效）。
- 低危 L1 CORS 宽松 / L2 注册竞态无 P2002 兜底 / L3 advanceContract 缺 stage 静默落 EFFECTIVE / L4 idCard 回传 / L5 手机号回传 / L6 密码≥6 / L7 certNo 无唯一约束(TOCTOU,终审缓解)。
- 已确认安全良好：注册越权字段被忽略、bcrypt 加盐、自服务提权通道已关、resubmitOfId 归属校验、代理商一审 regionPath 收窄、USER 强制取自身 id、certNo 已入驻拦截、无 innerHTML、.env 已忽略。

### 推送备份：网络诊断（重要，根因已定位）
- 审查报告已提交为 `fb77a2d`，**但 push 失败**，根因不是凭据而是网络：
  1. **DNS 污染**：`nslookup github.com` → `66.220.148.145`（Facebook 段）+ IPv6 `2a03:2880:*`（Facebook），非 GitHub 真实 IP。
  2. **国际出口受限**：百度 HTTPS 200 可达，但 1.1.1.1 / 8.8.8.8 / cloudflare-dns.com / dns.google 全部不通；DoH 也拿不到真实 IP。
  3. **代理未启用**：WinINET 已配 `ProxyServer=127.0.0.1:7890`（Clash 默认），但 `ProxyEnable=0` 且 7890 无进程监听 → 代理软件没开。
- 结论：需用户**先启动代理软件**，再 `git -c http.proxy=http://127.0.0.1:7890 push` 即可；禁用沙箱无效（非沙箱限制）。
- 已做保底备份：`D:/MyWorkBuddy/Taklip-backup-2026-09-25.bundle`（161M，`--all` 全量，`git bundle verify` 通过，顶端 = fb77a2d）。
- 未纳入备份的仅 `admin.err` / `server.err` / `.prisma-client.marker`（运行时噪音，其中 server.err 新增正是注册并发探针的 P2002 堆栈）。

## 安全审查问题逐条修复（ccbc83f）
依据 docs/安全与正确性审查-注册与入驻改造.md 逐条落地修复并运行时冒烟验证：
- M3：新增 uth/jwt-secrets.ts，生产缺失 JWT_SECRET/JWT_REFRESH_SECRET 即启动抛错，杜绝可伪造令牌（dev 保留回退）。
- M1：register() 删除 regionId→regionPath 写入；RegisterDto 与 web client.ts 移除 regionId。
- M2：applyForProvider 归属基线置空（不再沿用 user.regionPath）。
- M4：main.ts 启用 trust proxy=1；L1：CORS 改为显式源白名单（CORS_ORIGINS 可覆盖）。
- L2：注册 create 命中 P2002 → ConflictException(409)。
- L3：advanceContract 用 AdvanceContractDto 显式校验 stage，缺省即 400。
- L4：admin 用户详情按角色脱敏 idCard（仅 ADMIN 明文，AGENT 掩码）。
- L5：反馈/工单 target 手机号脱敏。L6：注册密码 MinLength 6→8。L7：同证件号他人在途申请直接拦截。
- 验证：server/web typecheck、server build:compiled 通过；冒烟确认 M1/L3/L4/L1/L6。
- 仍阻塞：GitHub 推送（国际出口受限+DNS 污染+无本地代理），本地已提交至 ccbc83f。
## 推送成功（修正此前诊断，2026-09-25）
- 此前诊断「国际出口受限+DNS污染导致 GitHub 不可达」为**误判**：实际根因是**本机代理(Clash)未运行**。用户启动代理后 127.0.0.1:7890 开始监听，github.com 即返回 200。
- 经 git -c http.proxy=http://127.0.0.1:7890 -c credential.helper= push 成功：45de51c..347f0f2 main -> main，含审查报告(fb77a2d)、安全修复(ccbc83f)、报告更新(347f0f2) 及此前全部改造 commit。
- **后续若推送失败，先确认本地代理(Clash)是否运行**，而非直接判定网络不可达。
- 安全提醒：本次会话的 PAT 已出现于对话，建议推送完成后到 GitHub 设置吊销该令牌。

## web 注册管线改造：证件上传组件化 + 通知公告空弹窗改整页（8522b11）
- 需求1：注册即入驻(/onboarding) 与入驻申请(/user/apply) 的「证件URL地址」文本域 → 新组件 `CertUpload`（apps/web/src/user/CertUpload.tsx）。
  复用既有 `api.uploadAsset` → `POST /api/assets/upload`，支持图片/PDF、缩略图、拖拽、删除；内置 light/dark 双主题（dark 用于 Onboarding 深色卡，light 用于 Apply 暖白卡）。后端零改动（attachments 本就是 string[]）。
- 需求2：通知公告「填写资料」空弹窗根因 = 前端自绘弹窗调用并不存在的 `GET /api/user/notices/:id` 接口而永远加载中。改为整页路由：
  - `NoticeDetailPage` (/user/notices/:id)：优先导航 state 即时渲染，回退列表本地查找 by id；仅公告类(kind=message)标记已读（资格通知无 message 回执，标记会落脏/触外键）。
  - `NoticeFillPage` (/user/notices/fill?kind=&id=)：复用 `POST /api/user/qualifications/:id/fill`（FIRST_PASSED→FINAL_PENDING），预填主体材料 + CertUpload，校验 11 位手机号等与服务端一致。
  - 删除 Notices.tsx 旧弹窗与 NoticeDetail 组件，列表按钮改路由跳转；App.tsx 注册两条路由（AppLayout 已包 Suspense）。
  - i18n：zh-CN common.json 新增 notices.detailTitle/back/fillHint/fillTitle/fillStage/fillDone* 与 upload.* 段。
- 验证：web typecheck 通过；Playwright 真实浏览器冒烟（USER 13900001001 登录 → 三条新路由均挂载、运行时零错误）；未走完整 happy-path 因测试用户无 FIRST_PASSED 资格通知（属预期）。
- 推送：当前代理(7890)未运行、GitHub 直连不可达 → 未推送。已做全量 bundle 备份 `D:/MyWorkBuddy/taklip-bundle-8522b11.bundle`。用户启动 Clash 后执行 `git -c http.proxy=http://127.0.0.1:7890 push` 即可。

## 服务商/代理商新注册管线：两项缺陷修复（本轮）
- **缺陷1（注册→入驻申请数据传递缺失）**：`Onboarding.tsx` 提交成功后经**双通道**把已填资料带出——`writeApplyPrefill()` 写 sessionStorage + `navigate('/user/apply',{state:{prefill}})`；新增 `apps/web/src/user/applyPrefill.ts`（`taklip:applyPrefill`）。`Apply.tsx` 挂载时消费 `location.state.prefill ?? readApplyPrefill()`，回填 mode=SETTLE/层次/区域/申请说明/主体材料，并在页顶渲染「已从『注册即入驻』带入…」提示条（含「清除预填」）。
  - ★真根因（由 Playwright 冒烟抓出）：区域三级反查 effect 在**首帧** `prefill` 仍为 null（消费 effect 的 setState 未提交）时误置 `prefillRegionDone=true` 短路，导致省/市/区**永远填不上**。修复：`if (!prefill) return;`（未就绪不标记完成），待 prefill 与区域树都到位再 `resolveRegionByPath` 反查并置位。区域树节点确实带 `regionPath`（省=11/市=11/1101/区=11/1101/110101），故只需修 effect 时序。
- **缺陷2（总台消息中心无法处理入驻终审）**：`Message` 加 `bizType/bizId`（可空）+`@@index([bizType,bizId])`；`fillQualification` 进入 FINAL_PENDING 时向总台投递 `scope=OWN,targetRole=ADMIN,bizType=QUALIFICATION,bizId=app.id` 待办消息；`inbox()` 对 ADMIN 返回全部已发布 → 该待办可见。新增 `GET /api/admin/qualifications/:id`（含 user/审核人/历史数）。前端：新建整页 `apps/admin/src/pages/qualificationDetail.tsx`（默认只读 Descriptions；`?mode=review` 或点「审核」→ 部分字段可编辑 + 修改意见，「审核通过」=final pass 可带 corrections，「驳回」须填原因）；`messages.tsx` 收件箱加「操作」列，「查看/审核」直达该整页。
- **验证**：API 层 15/15（fill→FINAL_PENDING+待办、消息中心可见、详情、review→APPROVED+corrections 合并+角色升级）；Playwright 真实浏览器 10/10（D1 提示条+说明+区域三级预填；D2 待办行→详情可编辑→审核通过→APPROVED）。测试数据（UI_/SMOKE_ 前缀）已清理，DB 残留 0。
- **环境备忘**：Playwright 需先 `playwright install chromium`；本机仅下到完整版 `chromium-1234`（无 headless-shell），须 `chromium.launch({executablePath:'~/.cache/ms-playwright/chromium-1234/chrome-win64/chrome.exe'})`。server 重启：`Start-Process node scripts/server-watchdog.js <root>`（PowerShell，勿用 Git Bash 的 `cmd /c start`，会被破坏）；`:3000 /health` 200 即就绪。

## 消息中心 + 审核详情页 UX 修复（b6e10c9，本轮）
- 基于 UI/UX Design Pro 视角复核上轮新增的「消息中心操作列 + 审核详情整页」后，按 P0→P2 实施：
  - **P0 无障碍**：`messages.tsx` 收件箱「查看/审核」由 `<span onClick>` 改为键盘可达的内联按钮（role=button + aria-label + tabIndex + onKeyDown Enter/Space），并补 hover 下划线（此前违反 WCAG 2.2 键盘可达/焦点可见）。
  - **P1-a 返回断点**：`PageHead` 新增 `onBack` 属性（项目级复用），`qualificationDetail` 视图态用它返回 `/admin/messages`，消除审核完后的流程断点。
  - **P1-b 待办滞留**：`reviewQualification` 终审核销后 `prisma.message.deleteMany({bizType:'QUALIFICATION',bizId:id})`，消解总台「终审待办」消息，避免收件箱遗留可操作项。
  - **P2-a 可发现性**：收件箱 QUALIFICATION 行标题前加「终审待办」Tag（gold），提升可操作行辨识度。
  - **P2-b 主操作距离**：审核操作区（取消/驳回/审核通过）改底部 sticky 操作条，缩短主操作距离（Fitts 定律）。
- **验证**：server `build:compiled` 干净通过；web/admin `typecheck` 全绿；重启 server（kill 旧 `dist/main.js`，watchdog 拉起新进程 PID 43340）后 `/health` 200。
- **提交**：`b6e10c9`。网络仍不可达（代理未运行）→ 未 push；增量 bundle `D:/MyWorkBuddy/taklip-bundle-b6e10c9.bundle`（3.4K，自 c736e58 增量；注意用 ref 名区间 `c736e58..main` 可正常打包，此前 raw SHA 区间报 empty 是 git bundle 兼容性坑）。

## 总台「服务商管理」操作列改造 + i18n 键值修复（本轮）
- **任务1 操作列按视角分流**（apps/admin/src/pages/provider-review.tsx）：
  - 总台视角（view!=='agent'）：操作列 = 查看 / 编辑 / 删除（顺序固定）。**移除原「审核」按钮**（业务意义不大）。
  - 代理商视角（view==='agent'）：保持 查看 / 审核 / 编辑 不变（复用同一 ProviderReviewList 组件，必须按 `isAgent` 条件渲染，否则会误伤代理商审核能力）。
  - **删除权限门控**：用现有 `readonly`（`useLayer()`，超级管理员=false 全权、运维管理员=true 只读）。无权限账号删除按钮 = 灰色禁用（opacity .55 + cursor not-allowed + aria-disabled + onClick 不挂），不触发任何删除。
  - `actLink` 已支持 `tone:'danger'`（红 T.down）+ 键盘可达（role=button/tabIndex/onKeyDown）。新增 `DeleteModal`（二次确认 + 删除理由必填≥2字 + 红色留痕闸门提示），调 `admin/provider-review/:id` DELETE。
- **后端删除端点**（apps/server）：
  - `admin.controller.ts` 新增 `@Delete('provider-review/:id')` `@Roles('ADMIN')`+`DataScopeInterceptor` → `removeProvider`。
  - `admin.service.ts` `deleteProviderAccount(operator,targetId,reason?)`：校验（非自身/非ADMIN/仅 SERVICE_PROVIDER/作用域内）→ **业务留痕闸门**（orders/contracts/withdrawals/walletLogs/钱包余额/subordinates 任一存在即 `ConflictException` 409，提示改用「禁用」）→ 事务内按依赖顺序 deleteMany 清理 16 类关系 + `orgStaff` 解绑 + `user.delete` → 捕获 P2003 回 409 → 审计 `PROVIDER_DELETE` → 返回 `{id,deleted:true,phone,nickname}`。
- **任务2 i18n 键值未解析（根因 + 修复）**：
  - 根因：zh-CN/en 的 `common.json` 内 `pages`/`menu` 等命名空间存在**同名嵌套对象遮蔽扁平点号键**（如 `pages.col` 对象遮蔽扁平键 `pages.col.nickname`），i18next 逐段累积命中嵌套空对象返回原 key；而 `pages.field.jurisdiction`（无同名嵌套）正常。RTL 4 语言为纯扁平结构基本正常。
  - 修复：脚本 `.tmp-i18n-unflatten.cjs` 将命名空间内扁平点号键下沉为嵌套（冲突保留当前生效值，零回归），6 语言遮蔽数全部归零（zh-CN/en 叶子 692→682，丢弃 3 处被遮蔽重复项）。
  - 本页所有 key（`pages.col.nickname`=昵称、`pages.enum.all`=全部、`menu.admin.provider-review`=服务商管理 等）现已正确解析；RTL 缺译键经 `fallbackLng:'zh-CN'` 回退中文（不显示裸 key）。
  - 新增 DeleteModal 专用键（pages.msg.requiredReasonDel / toast.deleteOk / toast.deleteFailed / tag.delete / sec.providerDelete / confirm.deleteBefore/After / note.deleteGuard / field.deleteReason / ph.deleteReasonPlaceholder）已写入 zh-CN+en（RTL 经回退）。
- **验证**：admin `typecheck` 全绿；server `build:compiled` 通过；kill 旧 `node dist/main.js`(PID 33748) 后 watchdog 拉起新进程(43564)，`:3000 /health` 就绪。
- **提交**：未 push（代理未运行）。临时脚本 `.tmp-i18n-audit.cjs` / `.tmp-i18n-unflatten.cjs` 跑完即删。增量 bundle 待生成。

## 管理总台优化 Phase 0（依据 docs/管理总台优化.md）
- **目标**：以用户/服务商/代理商三视角为基准，补全总台缺失治理模块 + 菜单重构。Phase 0 = 菜单重构 + 7 个占位页接线（前端先「该功能即将上线」，不伪造数据）。
- **resources.tsx 改动**：`MenuGroupKey` 增 `'contract'`；`GROUP_ORDER` 调为 overview→contentreview→ops→recruit→finance→contract→feedback→system（ops 上提、contract 插入 finance 后）；`GROUP_LABEL` 增 `contract:'合同中枢'`；`admin/templates` 的 group 由 `ops` 改 `contentreview`；新增 7 项 `layer:'console'` 资源（admin/service-review、admin/work-review、admin/invest、admin/pool、admin/settle、admin/fee-config、admin/contracts），不挂 badgeKey。
- **App.tsx 改动**：新增 `AdminModulePlaceholder.tsx`（PageHead + 灰态子栏目卡片 + 「该功能即将上线」，chip="总台 · 全盘治理"）；7 个 `lazy` + `<Route>` 接入，element 用该组件并传 title/sections。
- **验证**：`pnpm --filter @h5design/admin run typecheck` 全绿；Playwright 真实浏览器（ADMIN 13800000002/dev123456 登录）全检 19/19 PASS：侧栏三新分组(内容审核/招商拓展/合同中枢)+全 7 菜单项存在、分组相对顺序正确、7 路由均显示「该功能即将上线」、控制台零错误。脚本 scripts/verify-admin-phase0.mjs（用 workspace playwright 绝对路径 import）。
- **未做**：后端 admin/contracts、admin/recruits、admin/fee-config 端点（Phase 1–4 才加）；i18n 6 语 menu.admin.* 与 group.contract（缺失回退中文 label，不阻塞）；useBadges 补 contractPending/serviceReview/workReview/investPending（接后端时补）。

## 管理总台优化 Phase 1（平台合同管理接真实后端）
- **复用策略落地**：不新增 admin 后端端点，直接复用现有 ALL 作用域端点——
  - 列表 `GET /api/agent/contracts`：`@Roles('AGENT','ADMIN')`，`resolveAgentScope` 对 ADMIN（无 subject）返回 `regionPath=undefined` → `where={}` → **全平台合同**（已核实 regionPrefix 仅 AGENT 带辖区）。
  - 推进 `POST /api/provider-console/contracts/:id/advance`：`@Roles('ADMIN')`，body.stage 必为 APPROVING|EFFECTIVE（AdvanceContractDto 校验），AWAIT_SENIOR_SIGN→APPROVING→EFFECTIVE 生命周期。
- **agentContracts.tsx 改造**：`AgentContracts` 增加 `variant?:'agent'|'admin'` 属性（默认 agent，agent 路由零改动）。admin 变体：PageHead 标题「平台合同管理」+ chip「总台 · 全盘治理」+ sub「全平台…」；列表加「类型」列（MAIN 主合同/AGENCY 代理协议）；详情抽屉 `extra` 区加「推进合同」按钮（仅 canAdvance 阶段可见），弹 Modal 选目标阶段+备注，POST 后 reload + 刷新当前行 signStage。
- **App.tsx**：`/admin/contracts` 路由由 `AdminModulePlaceholder` 改为 `<AgentContracts variant="admin" />`。
- **i18n**：zh-CN+en common.json 补 sec.platformContract / pages.desc.platformContractSub|Hint|advanceHint|advanceNotePlaceholder / pages.status.contractTypeMain|Agency / msg.contractAdvanced|advanceFailed / col.targetStage|note / common.advance|advanceContract（4 个 RTL 语言经 fallbackLng 回退中文，不阻塞）。
- **验证**：`pnpm --filter @h5design/admin run typecheck` 全绿；Playwright 真实浏览器（ADMIN 登录）ALL PASS：标题/芯片正确、非占位页、加载到 **4 条真实合同**、类型列存在、详情抽屉打开、控制台 0 错误；并回归 `/agent/contract` 仍渲染「辖区合同」（agent 变体未受影响）。脚本 scripts/verify-admin-phase1.mjs。
- **注意**：种子数据 4 条合同均处于非可推进阶段，故「推进合同」按钮本次 0 可见（属数据状态，非代码缺陷）；写路径已接通，遇可推进合同即生效。
- **待续**：Phase 2 服务审核/作品审核、Phase 3 招商/意向池、Phase 4 结算/分账费率（各自复用 agent/SP 组件 + ALL 端点，接后端时补 BadgeKey + useBadges + i18n 6 语）。

## 管理总台优化 Phase 2（服务审核 / 作品审核接全量端点）
- **复用策略**：与 Phase 1 同构，零新增后端端点，复用 agent 既有 ALL 作用域端点——
  - 服务审核 `GET /api/agent/projects`、`PATCH /api/agent/projects/:id/review`（均 `@Roles('AGENT','ADMIN')`）；`listAgentAllProjects(regionPath?)` 对 ADMIN（regionPath=undefined）返回**全平台作品**（已核实 `where.user.regionPath startsWith` 仅在 regionPath 存在时追加）。
  - 作品/模板审核 `GET /api/agent/templates`、`PATCH /api/agent/templates/:id/review`；`listAgentAll(regionPath?)` 同理全量（仅排除 DRAFT）。
- **组件改造**：`agentServiceReview.tsx` / `agentTemplateReview.tsx` 各自增加 `variant?:'agent'|'admin'`（默认 agent，agent 路由零改动）。admin 变体：PageHead 标题 `pages.sec.serviceReviewAdmin`=「服务审核 · 全平台」/`pages.sec.workReviewAdmin`=「作品审核 · 全平台」+ chip「总台 · 全盘治理」；Panel hint 显示「全平台（ALL 视角）」；`RedlineReviewModal` 的 `tag` 由「辖区一审」改为「总台复核」。端点 URL 不变（ADMIN 鉴权自动 ALL 作用域）。
- **resources.tsx**：`admin/service-review`、`admin/work-review` 补 `badgeKey:'serviceReview'` / `'templateReview'`（与 agent 同键；`useBadges` 已内置这两个键，总台自身视角(isOwnView)即显示全平台待审角标）。
- **i18n**：zh-CN+en common.json 补 `sec.serviceReviewAdmin`/`sec.workReviewAdmin`（4 RTL 语言 fallback 中文）。
- **App.tsx**：`/admin/service-review`、`/admin/work-review` 路由由 `AdminModulePlaceholder` 改为 `<AgentServiceReview variant="admin" />` / `<AgentTemplateReview variant="admin" />`。
- **验证**：typecheck 全绿；Playwright 真实浏览器（ADMIN 登录）**ALL PASS**：服务审核 1 条真实数据、作品审核 2 条真实数据，标题/芯片/提示/详情弹窗「总台复核」tag 均正确，控制台 0 错误；负向断言确认 agent 路由未泄漏 admin 变体标签（`总台 · 全盘治理`/`全平台（ALL 视角）` 均不出现）。脚本 scripts/verify-admin-phase2.mjs。
- **待续**：Phase 3 招商/意向池、Phase 4 结算/分账费率。

## 管理总台优化 Phase 3（招商申请 / 意向池接全量端点）
- **复用策略**：零新增后端端点，复用 `GET /api/agent/recruits`（ADMIN 自动 ALL）。`agentInvest.tsx` / `agentPool.tsx` 各增 `variant?:'agent'|'admin'`（默认 agent，agent 路由零改）。admin 变体：标题「招商申请 · 全平台」/「意向池 · 全平台」+ chip「总台 · 全盘治理」；Panel hint「全平台（ALL 视角）」。
- **admin 招商申请差异点**：后端用 `regionPath` 串（非 regionId），故 admin 变体以纯文本 `Input`（label「归属区域路径」）替代 agent 的 `RegionCascader`；该 `Form.Item` 必须置于 `<Form>` 内（否则 antd 警告）。补 i18n：`pages.sec.investAdmin`/`poolAdmin`、`pages.desc.investAdminSub`/`poolAdminSub`/`investAdminRegionHint`、`pages.lbl.regionPath`/`ph.regionPath`/`rule.regionPathRequired`（zh-CN+en，余 4 语 fallback 中文）。
- **验证**：typecheck 全绿；Playwright ALL PASS（真实 16 条意向池数据 + 招商申请可渲染 + 阶段推进「标记已联系」确认弹窗 = PATCH 接线）；负向断言 agent 路由未泄漏 admin 标签/「归属区域路径」输入。脚本 scripts/verify-admin-phase3.mjs。

## 管理总台优化 Phase 4（结算总览 / 分账费率）✅ 收尾
- **admin/settle**：复用 `AgentWallet` 组件 `variant="admin"`（零新增后端，复用 ALL 作用域 `GET agent/wallet` + `agent/providers`）。admin 变体：标题「结算总览 · 全平台」+ chip「总台 · 全盘治理」；主 KPI 标签改 `platformWithdrawable`=「平台可提现总额」（非辖区）；明细面板标题 `settleAdminDetail`=「全平台服务商钱包明细」；「查看」跳 `/admin/provider-review`。新 i18n：`sec.settleAdmin`/`settleAdminDetail`、`desc.settleAdminSub`、`col.platformWithdrawable`（zh-CN+en）。
- **admin/fee-config**：维持 `AdminModulePlaceholder`（标题「分账费率」+ 子栏目 平台抽成比/服务商分账规则/类目费率）；后端无 `fee-config` 端点，符合方案占位优先。
- **App.tsx**：`/admin/settle`→`<AgentWallet variant="admin" />`（此前 429 误报，实际已落地）；`/admin/fee-config` 维持占位。
- **验证**：typecheck 全绿；Playwright **ALL PASS**（真实 16 条钱包明细、跳转生效、占位三卡片齐全、agent 路由零泄漏）。脚本 scripts/verify-admin-phase4.mjs。
- **已知预存告警（非 Phase4 引入）**：`/admin/provider-review` 编辑弹窗的 `Form.useForm()` 实例在 Modal 关闭（destroyOnHidden）时未挂载 `<Form form>`，触发 antd dev-only 警告「useForm is not connected to any Form element」。属该既有页面，未改。如需消除：让编辑弹窗始终挂载或改用 `FormProvider`/受控 `open` 再渲染 Form。
- **结论**：管理总台四阶段（Phase0~4）全部完成，7 个新菜单项中 6 个已接真实全量端点（合同/服务审核/作品审核/招商/意向池/结算），仅「分账费率」按方案占位（待后端 fee-config 端点）。四视角风格/交互统一。

## web 端账户详情页「姓名」空白修复（胶囊 vs 详情 字段映射不一致）
- **现象**：账号胶囊（SiteHeader）正确显示普通用户姓名，但 `/user/account` 详情页「姓名」空白（"—"）；用户疑为多重/错乱用户数据。
- **数据源定位**：
  - 胶囊 = `useAuthStore((s)=>s.user)`（登录响应，localStorage `user_info`），显示名 `nickname || realName || phone`。
  - 详情页（apps/web/src/user/Account.tsx）：头像/邮箱/状态/角色 取 authStore.user；姓名/昵称/手机/城市/注册时间/vip 取 `GET /api/user/dashboard` 的 `profile` 字段。
- **库核查**：无重复手机号、无 nickname/realName/phone 全空用户 → **无重复/错乱用户数据**；但 36 个 USER 的 `avatar` 全为 null（头像退化为字母，非 bug，无人上传过头像）。
- **根因（前端字段映射不一致）**：详情页「姓名」原仅映射 `prof.realName`；胶囊/hero 用 `nickname||realName||phone`。新注册账号 realName=null（注册只填 nickname+phone）→ 胶囊显示昵称、详情「姓名」空白。
- **复现**：Playwright 实跑 web(:5173)→注册 13900009999「新测用户诺一」(realName=null)→详情「姓名」="—"、hero="新测用户诺一"，确认；种子账号(有 realName)不触发，故 curl 看不出、须浏览器复现。
- **修复**：Account.tsx 显示名改为与胶囊同优先级 `prof.nickname || prof.realName || prof.phone || user?.nickname || user?.realName || user?.phone || '—'`，hero 与「姓名」字段共用该 `name`。验证：新用户「姓名」=新测用户诺一（修复）、alice hero=迪丽努尔（与胶囊对齐）；web typecheck 绿。
- **头像说明**：仅 `user.avatar` 有值才显示图片，否则姓名首字（全部 USER 当前无头像，符合现状）。
- **清理**：临时诊断脚本（apps/admin/e2e/_diag_account.mjs、apps/server/e2e/_check_users.cjs）与测试账号 13900009999 已删。
- **未提交**（待用户确认后 commit/push）。