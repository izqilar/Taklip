# 2026-08-19 工作日志

## 婚礼请柬 5 大高频组件：全部完成并验证
- 共享层（schema.ts / constants.ts / registry.ts / i18n）此前已就绪。
- 本次补齐 timeline、messageBoard 两个三件套（Canvas/DOM/Property），countdown/mapNav/like 在更早会话已完成。
- 接驳点：EditorCanvas.tsx、DOMRenderer.tsx、ComponentSettingsPanel.tsx 三个 switch 各加 5 个 case；ComponentLibraryMenu.tsx 的 5 个菜单项（countdown/mapNav/messageBoard/timeline/like）补上 `elementType`（此前点击是 no-op，因为 COMPONENT_ITEM_MAP 里只有 key 没有 elementType）。
- 修复 CanvasMapNav.tsx 第 42 行重复 `width` 属性导致的 TS17001 编译错误（pre-existing bug）。
- 验证：`pnpm --filter @h5design/web typecheck` 通过；`vite build` 通过（386 模块 transformed，仅 chunk 体积警告，与本任务无关）。
- 关键模式：Canvas 组件用 `{...common} offsetX/offsetY=中心` + Group 上清空 shadow；DOM 组件用 `commonStyle` + `dataAttrs`；Property 组件 `update(patch)/commit()` 签名。

## 模板管线规划文档（Phase 2 回顾）
- 文档已存 `docs/template-pipeline-design.md`（整合两轮规划 + 代码实测进度评估）。
- 实测：P0（三层角色/审核台/购买变现/钱包/路由）已落地；P1/P2 大部分已落地。
- 4 处偏差 + 待办任务已建跟踪：#254 导出图片(html-to-image)、#255 内容安全、#256 违规下架与申诉、#257 官方模板直 APPROVED（createByDesigner 按 role 分流）。
- 注：build 时 vite 清空旧 dist 会触发 genie-safe-delete shim 报错，需先 `rm -rf apps/web/dist` 再 build（环境 quirks，非代码问题）。
