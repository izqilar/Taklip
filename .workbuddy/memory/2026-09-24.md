# 2026-09-24

## 入驻申请双隧道 —— M5 验证收口（Playwright 全链路）
- 新增 `apps/admin/e2e/_verify_join_tunnel.mjs`（48 项断言全绿）：web 提交 → 服务商队列查看/接收/拒绝 → 用户业务消息回执 → 代理商同构 → 总台入驻审批台。
- **修真实 bug**：web `MessageDetail` 调 `GET /api/user/messages/:id`，服务端从未实现该路由（404），详情弹窗永远停在「加载中…」。已在 user-console.controller 补 `@Get('messages/:id')`（与列表同口径过滤含 scope=USER，编号按可见列表序号 M-10xx，不可见一律 404）；`build:compiled` + 重启 :3000 生效。
- Playwright 选择器事实：antd 两字按钮渲染为「关 闭 / 接 收」（中间插空格），必须 `/关\s*闭/` 匹配；关闭的 Modal 残留 DOM，须 `.ant-modal-wrap:visible` 过滤；web 页头语言切换是 `<select>`，区域三联动必须按 option 文案定位。
- 业务规则确认：已是成员再申请同团队 → 400「你已是该团队成员」（合理拦截，已入断言）；拒绝详情必填（空提交被拦截）。
- ★环境注意：本机共享 dev 库存在**并行清理行为**——alice/bob 的 TeamJoinApplication、OrgStaff、scope=USER 消息会在验证结束后几分钟内被清掉（与 /tmp/cleanup_join.cjs 旧清理脚本口径一致；server.log 还出现过本会话未发起的 `subject=e2e_agent_bazhou` 请求）。疑似另一 WorkBuddy 会话/人工在共享环境操作。跑验证要「跑完立即读结果」，别依赖数据留存。
- Prisma 脚本坑：ad-hoc 脚本查库必须 `import { PrismaClient } from './prisma/prisma-client/index.js'`（generator output 是自定义目录 `prisma/prisma-client`，根 node_modules 的 @prisma/client 是陈旧壳）；删 scope=USER 消息前要先删 `messageRead`（FK）。
- 验证后测试数据已清理（成员回到种子 4 人）；三端 typecheck 全绿。
- 待办：全部改动未 commit（等用户确认；推送受凭据网关限制）。

## 2026-09-24 · 个人中心菜单对齐（web ↔ 运营端用户视角）
- web 端 `USER_CENTER_NAV`：入驻申请与账户详情位置调换 → 个人中心组顺序 = 钱包/优惠/入驻申请/账户详情。
- 运营端：resources.tsx 用户视角新增 `user/apply`（group personal，UserAddOutlined，与 web 同序）；App.tsx 路由 `/user/apply`；新建 `pages/user/UserApply.tsx`（与 web Apply.tsx 同构：身份卡/我的申请/双隧道表单/说明+提交，readonly 联动——用户视角默认只读，权限模式切超级管理员后可代提交）。
- 服务端零改动：POST user/join-applications & qualifications 早已支持 body.userId 代理（resolveUserId）。
- i18n：web 的 userCenter.apply.* + menu.apply 注入 admin web-common.json（zh-CN/en；其余 4 语言无 userCenter 段，走 zh-CN 回退）；admin common.json 6 语言 menu 段追加扁平键 `user.apply`（LayerSider 源）。
- ★行尾符坑：admin 语言包 HEAD 里 common.json=LF、web-common.json=CRLF，脚本统一写 \n 会把 git diff 变整文件——同步脚本必须检测 dominant EOL 并按原格式写回；web-common.json 有行内紧凑写法，不能整份 json.dump → 用「括号匹配 + 在最后一个成员后插入」的文本级注入（_sync_apply_i18n.py，以 HEAD 为基底重建保证幂等）。
- ★Playwright 坑（运营端）：①layer.view 不能用 localStorage 注入（identity 校正回自身视角）→ 点顶栏「用户视角」按钮；②objectScope 仅存内存（scopesByView），page.goto() 会清掉它 → SPA 内点侧栏导航；③AutoComplete 的 input class 是 ant-select-selection-search-input 且 placeholder 不透传 → 用 [data-testid="objscope-search"] input；④多 dropdown portal 共存时点 `.ant-select-dropdown:not(.ant-select-dropdown-hidden)` 的 `.last()`，否则命中隐藏 option 报 not visible。
- ★JSX 语法坑：注释不能写在 JSX 属性列表中间（`<Comp {/*..*/} prop />` 是语法错误，vite 编译崩导致整站白屏）。
- 顺手修：ObjectScopeBar `onDropdownVisibleChange` → `onOpenChange`（antd 5.25+ 弃用）。
- 验证：`_verify_menu_align.mjs` 23/23（web 顺序断言、运营端菜单/页面/只读提示、代提交→待接收→撤回闭环、控制台无错误）；admin/web typecheck 绿；未提交。
- ⚠️ 并行会话仍在共享此环境（admin-dev.log 出现本会话未做的 providerPages/editor HMR 更新）。

## 工作交接（下一轮衔接）— 代理商工作台标准化
- **本轮已收尾**：①入驻申请双隧道全链路 M1–M5 验证 48/48（含修 `GET messages/:id` 404 真 bug）；②web↔运营端用户视角个人中心菜单对齐（账户详情↔入驻申请互换、运营端新增 `user/apply` 页/路由/6语言 i18n、顺手修 ObjectScopeBar 弃用 API）；菜单对齐验证 23/23，三端 typecheck 绿。
- **待完善（pending，非阻塞）**：①全部改动未 git commit（推送受凭据网关限制，需用户给 token）；②ug/kk-CN/ky-CN/uz-CN 的 web-common.json 仍缺 `userCenter.apply` 段（走 zh-CN 回退，功能正常）；③共享环境有并行清理/写入，验证数据不留存。
- **下一轮目标**：代理商（AGENT）工作台业务/菜单/页面标准化设计与优化。开场步骤：先盘点 AGENT 视角现有菜单树+页面形态 → 对照 `UI_Design/index.html`+`docs/` 还原文档列差距 → 与用户对齐菜单结构/命名/i18n/Modal→整页范围 → 分步实施+验证。AGENT 视角铁律：`view==='agent'===自身工作台，直接展示自身数据，绝不强制 subject`。
- **详细交接文档**见 `.workbuddy/memory/HANDOFF-2026-09-24.md`（含已完成清单、pending、下一轮步骤、可复用技能 console-agent-module / admin-modal-to-page-migration / console-view-restore、已知坑、关键文件索引）。

## 代理商工作台标准化方案（规划，未执行）
- 产出 `docs/代理商工作台标准化设计方案.md`：基于「用户/服务商菜单为基准 + 业务管线逻辑」双约束，对 AGENT 视角做标准化重排与查漏补缺。
- 现状：AGENT 仅 11 项且大部分业务项未分组；原型 `MENU.agent` 已规范 7 组 20 项。
- 目标树：数据看板/辖区服务商(辖区服务商·入驻审批·资质审核·合同管理·服务监管)/辖区运营(辖区用户·辖区订单·服务评价·意见反馈)/招商拓展(招商申请·意向池)/财务中心(结算总览·提现初审)/消息中心(通知公告·业务消息)/工作台设置(团队管理·员工角色·业务设置·账户详情)。
- 净增 10 项、改名 3 项（结算与钱包→结算总览、评价与反馈中心→服务评价、消息中心→业务消息）、补分组 7 项。内容页多数可复用既有共享组件（provider-review/qualificationReview/withdrawals/SPComplaints/SPContract/SPNotices/settings），仅招商申请/意向池/业务设置需新建后端端点。
- 第 10 节列了 5 个待用户确认的决策点（钱包语义、评价/消息拆分、招商等 3 项是否本轮、角标真实统计）；等用户评审通过再分批执行。

## 代理商方案 v2 修订 —— 补「内容审核闸口 / 红线把关」（未执行，待评审）
- 用户要求：服务商业务上架 + 模板上架**必须经代理商内容审核闸口**，尤其模板严把法律/意识形态红线。复核 v1 方案发现严重不足：仅笼统「服务监管」一项，且写"复用 templates.tsx 切 AGENT 辖区"——但核对后端发现这是架构级漏项。
- **后端事实（已核对）**：①模板人工审核端点 `PATCH /api/templates/:id/review` 仅 `@Roles('ADMIN')`，**代理商无权审**；`agent-console` 无模板审核端点。②服务/作品（`Project`）`publish` 直接置 `published`，**无任何审核闸口**，SP 一发即公开。③`ContentSafetyService` 仅关键词机审（词库硬编码、图片审核 placeholder 空实现 `return {passed:true}`、无人工红线复核、无审核留痕）。
- v2 修订：把「服务监管」拆为**模板审核 + 服务审核**两条独立管线，新建「内容审核」分组；补全 **发布闸口机制（待审态→通过才公开）+ 机审→人审红线清单复核工作流 + 两级权责（代理一审/总台抽检终审）+ 留痕审计 + 申诉扩展 + 红线词库 DB 化**；目标树扩到 8 组 21 项（净增 11、改名 3、补分组 7、新分组 1）；新增 Phase B0 后端改造阶段（agent 审核端点 + Project 待审态 + ContentSafetyService 升级）。
- 文档已重写 `docs/代理商工作台标准化设计方案.md`；第 12 节列 8 个决策点，其中 6/7/8 为红线强相关（是否双重审批、是否新增 Project 待审态、红线词库是否本轮 DB 化）。等用户评审通过再执行。

## 代理商方案 v2 执行 —— Phase A→D 全量落地（已完成）
- 用户拍板决策点 6/7/8：内容审核=代理一审+总台抽检（不双重审批）；服务审核本轮新增 Project 待审态+改造发布流程；红线词库本轮 DB 可配置（总台可维护），图片机审留接口。指令：按第 9 节 Phase A→D 分批执行。
- **Phase A（菜单/角标/i18n）**：resources.tsx 重排 AGENT 为 8 组 21 项，新增 `BadgeKey`(onboarding/qualification/templateReview/serviceReview/agentComplaints/withdrawReview/investPending/agentMessages) 与 `MenuGroupKey`(providersupervise/contentreview/regionops/recruit)；useBadges.ts 的 MenuBadges 接口补 8 键 + ZERO/映射。
- **Phase B0（后端内容闸口）**：①ContentSafetyService 升级——词库 DB 化（RedlineWord 表，空则播种 BUILTIN_REDLINE_WORDS）、scanContent(name,schema) 返回命中词/类别/字段、getRedlineCategories/refreshWordCache/scanImage 接口；②Template 模型加 reviewStage/redlineCategory；③Project 模型加 reviewStatus(@default approved)/reviewedBy/reviewedAt/reviewStage/redlineCategory/reviewNote；④新建 RedlineWord 模型；⑤publish.service 改造：SP 发布→机扫命中则 draft+rejected+SYSTEM 留痕，否则 review_pending（不公开）；非 SP 直接 published；新增 agentReviewProject（approve→published/approved；reject→draft/rejected+红线）；⑥template.service 加 listAgentPending/listAgentAll/agentReview（辖区校验）；⑦agent-console.controller 加模板+作品审核端点（resolveAgentScope 辖区过滤）；⑧console.module 引入 TemplateModule。
- **Phase B（前端审核页）**：新建 RedlineReviewModal（驳回强制意见+红线类别 CheckableTag，复用 antd Modal/Button，T.border 修正）、agentTemplateReview.tsx、agentServiceReview.tsx、agentComingSoon.tsx；App.tsx 加懒加载路由（template-review/service-review 真页 + 入驻/资质/合同/投诉/公告/招商/意向池/提现初审 placeholder，settle 复用 AgentWallet）；i18n 补 menu.agent.* / group.* / pages.sec.serviceReview / pages.col.* / pages.enum.* / pages.lbl.* / pages.toast.redlineRequired / pages.note.comingSoonHint（zh-CN/en）。
- **Phase C（角标后端）**：console-dashboard.controller badges 方法按 AGENT scope 统计 8 新键（onboarding=资质/入驻待审、qualification=资质待审、templateReview=辖区 PENDING 模板、serviceReview=辖区 review_pending 作品、agentComplaints=辖区 REGION 工单、withdrawReview=辖区待审提现、investPending=招商意向待处理、agentMessages=辖区 REGION 未读消息）；regionPath startsWith 辖区收敛。
- **Phase D 防御纵深（后端强校验）**：新增 assertRejectRedline(cat,note) 于 content-safety.service，在 template.review / template.agentReview / publish.agentReviewProject 三处驳回分支强制「必填意见 + 有效红线类别」，否则 BadRequest（前端已强制，后端兜底防绕过直接调 API）。
- **验证**：①三端 typecheck 全绿（admin/server/web）；②后端 E2E（apps/server/e2e_gate.js 临时，已删）35/35——SP 发布→review_pending 不公开、agent 辖区可见、approve→published/AGENT 留痕、reject 带类别→draft/类别留痕、**无类别驳回被 400 拦截**、8 角标键齐全、模板管线 PENDING→AGENT 审批；③Playwright 走查（临时脚本已删）WALKTHROUGH_PASS=true——agent 登录直达 /agent/dashboard、侧栏含 模板审核(1)/服务审核(1) 角标、两新页无白屏无 console/page error。
- **环境操作**：:3000 此前未起且 dist 陈旧 → `prisma generate` + `prisma db push`（加性，无破坏迁移）+ `build:compiled` + 经 watchdog 重启（新 PID 33876）生效；:5174 已在跑。
- **未提交**：全部改动未 git commit（推送受凭据网关限制，需用户给 token）。本轮属代码生成/重构验证，未新建可复用 skill。
