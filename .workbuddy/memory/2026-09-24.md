# 2026-09-24 · 代理商工作台续做（5 占位页落地 + 角标对齐）

## 本轮交付（commit b51167f）
- 前端新增并接入路由的 5 个代理商页：
  - `agentQualifications`（入驻审批·代理一审：FIRST_PENDING→FIRST_PASSED/REJECTED，**不触碰身份变更**）
  - `agentWithdrawReview`（提现初审：reviewStage AGENT_PENDING→AGENT_PASSED/REJECTED，**不触碰资金闸门**）
  - `agentContracts`（辖区合同·只读，含服务商信息）
  - `agentInvest`（招商申请·登记漏斗 + 最近登记，regionPath 由后端辖区推导）
  - `agentPool`（意向池·阶段推进 NEW→CONTACTED→WON/LOST）
- 后端 `agent-console.controller.ts` 新增 8 个辖区隔离端点（GET/PATCH qualifications、GET contracts、GET/PATCH withdrawals、GET/POST/PATCH recruits）。
- schema：新增 `RecruitLead` 模型；`Withdrawal.reviewStage` 一审标记；**`ProviderContract` 补 `provider` 关系 + User 反向 `contracts`**（修复 contracts 端点 `include:{provider}` 此前因缺关系而 never/运行失败）。
- `console-dashboard.controller.ts` 角标对齐：`onboarding`（口径扩至代理商层次 provider+agent）、`withdrawReview`、`investPending`（辖区内 stage=NEW 招商意向）现已按代理商辖区统计。
- i18n zh-CN/en 补全：`col.reason/certNo`、`pages.desc/ph/rule`、`pages.lbl.registerRecruit/recentRecruit/intentPool`、`pages.toast.actionFailed` 等。

## 验证
- admin + server 双 `typecheck` 全绿；server `build:compiled` 通过。
- 启动重建后服务，冒烟测试（ADMIN + 真实 AGENT test_agent_x/region 65/6501，密码 Test123456）：
  - 8 端点全部 200/201；POST recruit 写出的 regionPath 由辖区推导（=65/6501）、ownerAgentId 正确；PATCH 阶段落库 CONTACTED；角标 investPending/onboarding/withdrawReview 随辖区正确计数。
  - ADMIN 无 region 时 POST recruit 返回 400「代理商未配置辖区」——辖区隔离守卫生效。
  - 测试产生的招商意向已清理（deleteMany phone=13900008888）。

## 注意 / 待办
- **seed 数据 regionPath 格式不一致**：代理商用 `65/6501`（带斜杠），而 qualification 旧样本用 `6501`（无斜杠），导致 `startsWith` 辖区收敛对历史数据可能漏判。非本任务引入，需统一 seed 或补迁移；当前不影响新端点正确性。
- 远程推送仍被凭据网关拦截（fine-grained PAT 需 Contents: Read and write）；本提交仅本地 commit b51167f。
