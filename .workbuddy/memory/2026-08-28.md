# 2026-08-28

## 三层架构命名规范固化（用户端 / 服务端 / 运营端）
- **背景**：用户厘清「后端(3000)」与「管理后台(5174)」的二义性——5174 实为前端网页（admin 运营界面），3000 才是唯一服务端。此前"前端/后端/管理后台"三词混用易歧义。
- **定稿规范**（已写入 `MEMORY.md` 顶部「命名规范（必读）」段）：
  - 服务端 (Server) = `apps/server` = :3000
  - 运营端 (Admin Console) = `apps/admin` = :5174（ADMIN/AGENT/SP 内部/合作方界面）
  - 用户端 (User App / H5) = `apps/web` = :5173（USER，含 AgentStudio）
  - 旧称映射：前端(apps/web)=用户端、后端(apps/server)=服务端、管理后台(apps/admin)=运营端。
  - 约定：禁用"前端/后端/管理后台"旧称；提到其一须带「英文+包名+端口」三锚点。
- **落地范围**：
  - ✅ `MEMORY.md`：整文件重写，技术栈/架构/关键文件/开发环境/进度等全部用新称，并新增命名规范段（权威真相源）。
  - ✅ `2026-08-26.md`、`2026-08-25.md`：定向替换 管理后台→运营端、后端→服务端、前端(apps/admin 语境)→运营端、Refine 后台→Refine 运营端；"后台 node"等 background 含义的"后台"保留不改。
  - ⏸️ `2026-08-10`～`2026-08-24` 历史日志：保持 append-only 不回改；其旧称由 MEMORY.md 映射规则解读（盲目批量替换会错配 apps/web 与 apps/admin 语境）。
- **未做（可按需追加）**：`docs/admin-menu-restructure.md` 等开发文档仍用旧称，未强制改（用户仅要求项目记忆内统一）。如需全仓文档也统一，可再批处理。

## 运营端 UI 原型还原标准开发文档（v1.0）
- **背景**：用户提交 `UI_Design/ui-run/console/index.html`（已验收原型，单文件 SPA、手写 CSS+CSS变量、4 角色视角 JS 切换、4 首页+30 列表/详情页）+ 两份开发文档（A:`UI_Design/庆柬云·分层管理后台 开发文档.md` 设计契约；B:`docs/庆柬云·运营端开发文档（React+Refine+AntD5）.md` 技术翻译），要求按运营端技术栈重新整理一份全新标准开发文档用于"原样还原"。
- **三项决策**（用户拍板）：① 文档覆盖**完整 4 视角**（总台13/代理商8/服务商8/用户7，含用户视角 7 页）；② 设计令牌**以原型为准**（朱砂红 `#c24b2e`+青 `#14676b`+暖石 `#f5f2ec`，当前运营端代码 `#C81E42` 须更新）；③ **新建权威文档** `docs/庆柬云 · 运营端UI还原标准开发文档.md`。
- **产物**：`docs/庆柬云 · 运营端UI还原标准开发文档.md`（15 章 + 附录 A 原型→代码逐页追溯表 36 行 + 附录 B 实现任务清单）。整合 A 设计意图 + B 技术栈，并补 B 缺口：组件/目录规范章、对象聚合字段(AGENTS/PROVIDERS/USERS/TIERS)、首页看板字段级规格、原型→代码追溯附录、里程碑/测试策略。
- **关键事实**：原型 `MENU`/`PAGES` 键已核实（console: regions/agents/review/users/templates/orders/wallets/withdrawals/feedback/messages/roles/settings；agent: a-*；provider: p-*；user: u-*）；原型 CSS 变量 `--accent:#c24b2e --accent-2:#14676b --page:#f5f2ec` 为真值。
- **实现任务清单（附录 B，不在此阶段执行）**：① 主题 `#C81E42`→`#c24b2e`；② 角色枚举 bug `'SP'`→`'SERVICE_PROVIDER'`；③ 补依赖（Redis 客户端、admin 的 i18n/react-query/@h5design/core、图表库）；④ 补齐缺失端点（wallets/templates/roles/settings/agent/*/provider/*/user/*/view-scope，见附录 A ⚠）；⑤ agent/sp 共 14 页占位→真实；⑥ 图表还原（SVG→图表库）。
- **引用更新**：`docs/admin-menu-restructure.md` 增 §12 指向新权威文档（本文件降为过程文档）。
- **未做**：本文档仅产出 .md，未改任何源码；源码改造见附录 B，待后续 Craft 阶段执行。

## M1 实现：主题令牌 + 角色枚举修复（Craft）
- **范围**（标准文档附录 B①/②）：运营端 `apps/admin` 主题令牌 `#C81E42`→原型朱砂 `#c24b2e`，并按 §11.3 落地 antdTheme 全套令牌（青 `#14676b`、暖石 `#f5f2ec`/`#fffefb`、状态色、圆角6/控件高34）；修复 `accessControlProvider.canSeeByRole` 的 `'SP'`→`'SERVICE_PROVIDER'`（否则服务商中心 sp/* 对服务商角色不可见）。
- **改动文件**：
  - `apps/admin/src/App.tsx`：`theme` 对象升级为 `ThemeConfig`（引入 `import type { ThemeConfig }`），色板对齐原型；标题"庆"图标色 `#C81E42`→`#c24b2e`。
  - `apps/admin/src/main.tsx`：移除冗余 `ConfigProvider` 的 `theme`（统一由 App.tsx 单一主题源接管，仅保留 locale）。
  - `apps/admin/src/providers/accessControlProvider.ts`：第48行 `role === 'SP'`→`role === 'SERVICE_PROVIDER'`。
  - `apps/admin/src/pages/{withdrawals,wallets,users,provider-review,orders,dashboard}.tsx`：硬编码品牌色 `#C81E42` 全量替换为 `#c24b2e`。
  - `docs/admin-menu-restructure.md`：同款 `'SP'` 笔误伪代码 + 正文 `SP=SELF`→`SERVICE_PROVIDER=SELF` 同步修正（保持文档一致）。
- **校验**：`pnpm typecheck`（tsc --noEmit）通过，exit 0 无错误。运营端为 Vite 热更新，改动即时生效，无需重新构建。
- **未涉及（M2+）**：依赖补齐、缺失端点（api/admin/wallets 等）、14 页占位→真实、图表库还原、用户端 `apps/web` 仍保留 `#C81E42`（用户端品牌色，按规范不动）。

## M1 骨架：组件/目录规范 + 四层36资源 + 布局骨干（Craft，标准文档 §6/§7/§11.4/§15.2-M1）
- **范围**：按标准文档 §6.2 组件目录规范 + §7 四层36资源注册 + §11.4 全局 StatusTag + §4 视角切换/只读/对象视角 + §8.5 图表封装，落地运营端架构骨架（区别于上方附录B①②的「主题+角色枚举」小修）。
- **新增文件**：
  - `apps/admin/src/config/theme.ts`：§11.3 antdTheme（朱砂 `#c24b2e`/青 `#14676b`/暖石 `#f5f2ec`/`#fffefb`/状态色/圆角6/控件高34）+ 品牌分色 `BRAND`（庆红/云金）。App.tsx 改引此文件。
  - `apps/admin/src/config/resources.tsx`：§7 四层 36 资源注册（meta.layer/group/icon/badge + LAYER_HOME/LAYER_LABEL）。**注意 .tsx**（含 JSX 图标）。
  - `apps/admin/src/config/permGroups.ts`：§5.3 10 域 28 权限点 + `layerDomains()` 分层过滤（代理9域/服务商6域）。
  - `apps/admin/src/components/common/StatusTag.tsx`：§11.4 全局状态标签（#ok→success/#warn→warning/#bad→error/#mut→default/#ac→processing），覆盖四层全部状态枚举，杜绝英文键值。
  - `apps/admin/src/providers/layerContext.tsx`：视角 view + 只读 + 对象视角 context（登录即定层）。
  - `apps/admin/src/components/layout/{ReadonlyBanner,ObjectScopeBar,LayerHeader,LayerSider,AdminLayout}.tsx`：§6.1 三段式布局；LayerHeader=品牌+视角切换 Segmented(仅ADMIN)+对象检索+账号只读开关菜单；LayerSider 按当前视角从 config/resources 投影并 meta.group 分组。
  - `apps/admin/src/components/charts/{BarCompare,DonutShare}.tsx`：§8.5 轻量内联 SVG 条形/环形图（避免引入重型图表依赖，依赖补齐留待实现阶段）。
- **改造文件**：
  - `apps/admin/src/App.tsx`：用 `LayerProvider`+自定义 `AdminLayout` 替换 `ThemedLayoutV2`；`resources` 改引 config；新增路由 `/admin/templates`、`/admin/roles`、`/agent/roles`、`/sp/roles`、用户视角 7 页（暂 `PlaceholderPage`，后端 view-scope/user 端点留 M3）；`HomeRedirect` 按角色落点首页。
  - `apps/admin/src/components/GenericListPage.tsx` / `ConsoleView.tsx`：删除各自重复的 STATUS_LABEL，统一改用全局 `StatusTag`（format:'status'）。
- **校验**：`pnpm --filter @h5design/admin typecheck` EXIT=0；`pnpm --filter @h5design/admin build`（tsc+vite）EXIT=0（4115 模块，bundle 1.86MB）。本环境 Docker/Prisma 未起，未做浏览器实跑。
- **假设**：视角切换仅 ADMIN 可见（低层登录即锁定本层，无越层入口，符合 §4.1）；对象视角检索的 view-scope 聚合接口在 M3 接入，ObjectScopeBar 当前为交互骨架（标注"待接入"）。
- **下一步（M2/M3）**：总台层 templates/roles 真实页 + 用户视角 7 页真实实现（需后端 user/* 端点）+ agent/sp 首页看板接入 BarCompare/DonutShare + view-scope 后端端点。

## M2 总台层真实化（Craft，标准文档 §15.2-M2 · 模板审核/角色与权限真实页）
- **范围**：按文档 §8.1 把总台「模板审核」「角色与权限」两个占位页替换为真实实现；其余总台页（users/agents/providers/orders/withdrawals/wallets/feedback/messages/regions/dashboard）此前已接真实 `/api`，本轮回填缺口。
- **后端（apps/server）**：
  - `template/template.service.ts`：新增 `listAdmin(status?,keyword?,skip,take)` → `{items,total}`（含 author），供审核台拉全状态列表。
  - `template/template.controller.ts`：新增 `@Get('admin') @Roles('ADMIN')`（`/api/templates/admin`，先于 `:id` 路由），复用现有 `/:id/review`(approve/reject)、`/:id/takedown`。
  - **角色**：Prisma **无 `model Role`**（角色为 4 枚举 + 代码级 RBAC），故角色页不伪造后端，改为前端只读视图（见下）。
- **前端（apps/admin）**：
  - `pages/templates.tsx`（新建）：列(模板/作者/类型/价格/状态)+筛选(已上架/待审核/已驳回/已下架)+搜索；行操作 查看/通过(快捷)/驳回·下架(drawer)；type-B 审核抽屉含审核意见，驳回/下架必填原因；状态用全局 `StatusTag`（新增 `TAKEN_DOWN→已下架` 映射）。
  - `pages/roles.tsx`（新建）：列出 4 内置角色(ADMIN/AGENT/SERVICE_PROVIDER/USER)，列(角色/层级/数据作用域/权限域数/类型)；type-A 只读抽屉按 `layerDomains()` 过滤展示功能权限复选框组（源自 `config/permGroups.ts` 权威源）。编辑需 Role 模型，标注规划中、不伪造。
  - `pages/dashboard.tsx`：待办卡片改为可点击直达（入驻审核/提现/评价/模板审核），对齐 §8.5。
  - `App.tsx`：`/admin/templates`、`/admin/roles` 占位→真实组件。
- **校验**：`pnpm --filter @h5design/admin typecheck` EXIT=0；`@h5design/server typecheck` EXIT=0；`pnpm --filter @h5design/admin build` EXIT=0（4115 模块）。Docker/Prisma 未起，未做浏览器实跑。
- **诚实占位**：`/admin/settings`（系统设置）无 Role/Settings 模型且无迁移条件，保留 PlaceholderPage。
- **下一步（M3）**：agent/sp/user 三层 14 页真实化 + 对象视角 `view-scope` 后端 + 首页看板接入 `BarCompare`/`DonutShare`；用户视角 7 页需后端 `user/*` 端点。

## M3 三层真实化（Craft，标准文档 §15.2-M3 · agent/sp 图表 + 用户视角 + 对象视角）
- **M3-① 首页图表（后端+前端）**：
  - `console/chart-util.ts`（新）：`bucketMonthly`(近6自然月流水聚合) + `lastSixMonths` + `since180`(180天窗口)。
  - `agent-console.controller.ts` / `provider-console.controller.ts`：`dashboard` 增 `serviceCategoryShare`(模板按 category findMany+内存聚合) + `monthlyRevenue`(近180天订单按自然月聚合，单位分)。注：Prisma 该版本 `groupBy` 类型要求 `orderBy` 且 `_count._all` 不支持 → 改用 findMany+JS 聚合。
  - `ConsoleView` 增 `charts?:ChartDef[]`（donut/bar，支持 dataPath/labelKey/valueKey/valueScale/unit），渲染 `DonutShare`/`BarCompare`。
  - `AgentDashboard`：辖区服务类别占比(donut)+近6月辖区流水(bar,元)；`SPStudio`：我的服务类别占比(donut)+近6月我的收入(bar,元)。
- **M3-② 用户视角监督镜像（后端+前端）**：
  - `console/user-console.controller.ts`（新，`@Controller('api/user')`，@Roles('ADMIN','AGENT')+RolesGuard）：`resolve`(按手机/昵称检索，AGENT 辖区受限)、`dashboard`、`orders`、`providers`、`feedback`、`messages`、`wallet`；userId 缺省回落种子演示用户 13900001001。
  - **诚实约束**：无 Coupon/UserWallet 模型 → `coupons` 恒 0、`wallet` 对普通 USER 返回 isProvider:false 且余额全 0（不伪造）。
  - `console.module.ts`：注册 UserConsoleController。
  - `GenericListPage` 增 `staticFilters`（透传为 permanent 过滤，注入 userId）。
  - `pages/userPages.tsx`（新）：UserDashboard/UserOrders/UserProviders/UserCoupons(空态)/UserWallet/UserFeedback/UserMessages，读取 layerContext.objectScope.id 作为监督目标。
  - `App.tsx`：用户7路由换真实页；`agent/roles`、`sp/roles` 占位→复用 `RolesPage`（只读真实）。
- **M3-③ 对象视角（前端）**：`ObjectScopeBar` 由骨架改为真实——AutoComplete 调 `/api/user/resolve`，选中写 layerContext.objectScope，用户视角整页联动；`LayerHeader` 在 user 视角显示检索条。
- **校验**：`@h5design/server typecheck` EXIT=0；`@h5design/admin typecheck` EXIT=0；`admin build` EXIT=0（4119 模块）。Docker/Prisma 未起，未做浏览器实跑。
- **下一步（M4）**：A/B/C/D 四类审核详情抽屉（含权限复选框组）+ 审核幂等（当前 templates/provider-review 已有简易抽屉，待统一四类）；对象视角扩展到 agent/provider 目标检索（当前仅 user）；系统设置 `Role`/`Settings` 模型落地后可做角色编辑。

## M4 详情与审核流（Craft，标准文档 §15.2-M4 · 文档 §9 / §12.2-§12.3）
- **范围**：统一 A/B/C/D 四类详情对话框 + 权限复选框组 + 审核幂等 + 消息中心审核流。此前 templates/provider-review 各用 ad-hoc Drawer/Popconfirm，messages 用 window.prompt，待统一。
- **新增共享组件** `apps/admin/src/components/detail/`：
  - `PermCheckGroup.tsx`：权限复选框组，按 `layerDomains()` 分层过滤回显（代理9域/服务商6域/总台10域/用户0域），支持 checkedKeys 回显 + onChange 写回（抽自 roles.tsx）。
  - `ReviewActions.tsx`：类型 B 审核动作——通过(审核意见可选)/驳回(必填原因+校验拦截)/只读锁定/loading，提交后清空。
  - `DetailDrawer.tsx`：统一四类(mode: view/rereview/edit/editOnly)。A 仅查看+关闭；B 查看+ReviewActions；C 查看↔编辑(预填不空白,保存/取消)；D 仅编辑 Form。支持 `permGroup`(权限复选框组)、`review`(B 配置)、`edit`(C/D 配置)、`footerExtra`(任何模式页脚附加，如反馈状态流转)。状态字段统一走全局 `StatusTag`(§11.4)。
- **前端改造**（均改引 DetailDrawer）：
  - `templates.tsx`：模板审核→DetailDrawer(B)，下架仍走 Modal 必填原因；保留列表快捷「通过」。
  - `provider-review.tsx`：列表行「查看/审核」→DetailDrawer(B)，详情含申请人/手机号/已批准·待审服务/辖区/资质状态；通过/驳回走抽屉。
  - `roles.tsx`：→DetailDrawer(A)+PermCheckGroup(只读)，保留「Role 模型待落地」提示。
  - `messages.tsx`：audit tab 审核→DetailDrawer(B) 替代 `window.prompt`，通过并发布/驳回(原因必填)，正文白底保留。
  - `feedback.tsx`：处理→DetailDrawer(view)+footerExtra(处理意见 TextArea + 状态流转：升级转总台/认领协商/仲裁关闭)，只读态锁定。
  - `users.tsx`：本地 StatusTag 改为全局 `StatusTag`(value 入参)，对齐 §11.4 去重。
- **后端 审核幂等**（§12.3 STATE_CONFLICT=409，无 Redis 客户端→用状态机守卫替代 Redis 幂等键）：
  - `template.service.ts`：review/takedown 重复操作由 BadRequest→`ConflictException(409)`。
  - `admin.service.ts`：`approveProvider`/`rejectProvider` 加 `pendingServiceRoles` 空→`ConflictException(409)`；`rejectProvider` 增 `reason` 入参（User 模型无持久化列，仅审计留痕，待 Role/审核记录模型落库——诚实不伪造）。
  - `admin.controller.ts`：reject 端点解构 `@Body() { reason }` 透传。
  - `message.service.ts`：approve/reject 不在 PENDING→`ConflictException(409)`（reject 原因已落 `rejectNote`）。
- **校验**：`@h5design/admin typecheck` EXIT=0；`admin build` EXIT=0（4124 模块）；`@h5design/server typecheck` EXIT=0（沙箱禁用）。Docker/Prisma 未起，未做浏览器 E2E。
- **已知 deferred**：① provider-review 驳回原因暂无持久化列（同 Role 编辑，待迁移）；② 对象视角扩展至 agent/provider 检索（当前仅 user，见 layerContext.ObjectScopeBar）；③ 系统设置 D 类编辑待 Role/Settings 模型。

## M5 质量（Craft，标准文档 §15.2-M5 · i18n/RTL + 四态 + E2E）
- **范围**：按 §13 接国际化与 RTL；按 §11.5 补四态（加载/空/异常/只读）；按 §14 建 E2E 验收脚手架。
- **i18n/RTL**：
  - 依赖：`pnpm --filter @h5design/admin add react-i18next@15 i18next@23 i18next-browser-languagedetector@8`（已装，+4 包）。
  - 新增 `src/i18n/index.ts`（初始化：6 语言 zh-CN/en/ug/kk-CN/ky-CN/uz-CN；`RTL_LANGS=[ug,kk-CN,ky-CN,uz-CN]`；`isRTL()`；`SUPPORTED_LANGS`；`fallbackLng:'zh-CN'`）+ `src/i18n/locales/{zh-CN,en,ug,kk-CN,ky-CN,uz-CN}/common.json`（zh-CN/en 全量 chrome+menu+status+view；4 RTL 为 common+view 子集，menu/status 回退 zh-CN）。
  - 新增 `src/providers/i18nProvider.ts`：Refine `I18nProvider` 适配器（translate/changeLocale/getLocale）。
  - `App.tsx`：接 `i18nProvider`；新增响应式 `Shell`——`useTranslation` 取语言 → `ConfigProvider direction={isRTL?'rtl':'ltr'}` + antd locale(zh_CN/en_US)。`main.tsx` 移除外层 `ConfigProvider`（移交 Shell）。
  - `LayerHeader`：视角标签/只读/退出走 `t()` + 语言切换 `Select`（6 语言）。
  - `LayerSider`：菜单标签按资源名推导 `menu.*` 键走 `t()`。
  - `StatusTag`：状态文案走 `t()`（`status.*` 键，新增 `i18nKey` 字段）。
  - `GenericListPage`/`ConsoleView`/`DetailDrawer`/`ReviewActions`/`ReadonlyBanner`：按钮/状态/加载/空/异常走 `t()` 或四态组件。
- **四态**：新增 `src/components/common/EmptyState.tsx`、`ErrorState.tsx`（§6.2）。`GenericListPage` 接 `loading`(Table)/`error`(ErrorState+refetch)/`empty`(locale.emptyText=EmptyState)；`ConsoleView` 接 `error`(ErrorState+refetch)/`empty`(EmptyState)；`ReadonlyBanner` 文案走 `t()`。只读锁定落到写按钮：templates(通过/驳回·下架/违规下架)、provider-review(查看/审核)、messages(发布/审核)、feedback(发起/处理/提交) 均 `disabled={readonly}` 并经 `DetailDrawer readonly` 锁审核/编辑/权限组。
- **E2E 脚手架**：新增 `apps/admin/e2e/playwright.config.ts` + `apps/admin/e2e/admin.spec.ts`（覆盖 §14 全部 11 项：四层投影/视角联动/对象视角/只读/角色权限/RTL 等）；`docs/运营端M5验收清单.md`（§14 逐条映射 + 运行方式）。**未实际运行**（Docker/Prisma 未起，且 `@playwright/test` 未装；文档列 install+run 命令）。
- **校验**：`@h5design/admin typecheck` EXIT=0；`admin build` EXIT=0（4172 模块）；`@h5design/server typecheck` EXIT=0（沙箱禁用）。Docker/Prisma 未起，未做浏览器实跑。
- **诚实 deferred**：① 4 RTL 语言仅 chrome 子集（菜单/状态回退中文），页面级逐句翻译为后续接入项（文档 §13 已声明"后续接 6 语言"）；② E2E 需后端+浏览器实跑核验（环境未就绪）；③ 四态 error 态依赖后端返回错误，前端已接 `isError`/`refetch`，真实错误需联调。

## M5 收尾：页面级逐句翻译 + E2E 实跑核验（Craft，2026-08-28）
- **页面级逐句翻译（完成）**：把 17 页面 + 组件库残留的硬编码中文全部 `t()` 化。收尾包裹：consolePages(品类列/详情label)、users(未分配 fallback)、Placeholder(comingSoon + 加 import t)、messages(Toast 消息已提交/等待总台审核/并发布、审核 Tab 权威公告审核、Alert 三态提示、表单 请输入内容、详情抽屉标题)、templates(诞育庆贺 category、已通过上架/已下架/已驳回 Toast、模板审核标题)、orders(诞育庆贺 category)、provider-review/roles/feedback(审核/角色/反馈处理标题、反馈三态提示、请输入内容)。
- **新增 9 个 pages 字典键**（6 语言）：`col.category`/`cat.birthCelebration`/`note.publishHintAgent|Sp|Admin`/`note.fbAgent|Admin|User`/`msg.plsInputContent`（zh-CN/en/ug 全量，kk/ky/uz 英文占位=RTL 布局）；脚本 `temp/add_pages_keys.py` 可幂等重跑。
- **模块级全局 `t`**：`src/i18n/t.ts` 解决多组件文件 `t` 作用域（Shell 重渲染即实时切换）。
- **校验**：`@h5design/admin typecheck` 0 error；`admin build` 4170 模块通过。验收清单 §一.10 由 🔜 升 ✅（页面级翻译完成）。
- **E2E 实跑核验**：
  - 装 `@playwright/test`(1.62.1, devDep)；`playwright test --list` 11 用例可解析。
  - 浏览器修订坑：1.62.1 要 v1234，沙箱无 CDN 网络（下载卡死），仅缓存 v1228。用缓存 v1228 经临时 config 实跑（验证后已删临时 config）。
  - **选择器 bug 修**：登录按钮 accessible name 实为「登 录」(含空格)，精确 `name:'登录'` 超时；`admin.spec.ts` `login()` 改 `page.locator('button[type="submit"]')`。
  - **复跑**：提交成功 → `waitForURL('**/dashboard')` 超时；失败点隔离到后端 `:3000` DOWN（登录 API 不可达）。即工具链(浏览器+dev server+登录交互)已验证，完整 §14 跑通需后端+Docker 在线。
- **诚实结论**：E2E 脚手架与选择器就绪；本环境仅验证到登录交互层。正式 `playwright install chromium`(v1234) + 后端就绪后可全量跑。

## 运营端 UI 原型还原（收尾轮）：角色落地修复 + 客观还原度校验（Craft，2026-08-28）
- **本轮目标**：以 `UI_Design/ui-run/console/index.html` 为唯一视觉真值，把 apps/admin 还原到位并给出可验证结论。
- **新增客观校验工具（已归位 `apps/admin/temp/`，可反复重跑）**：
  - `verify-ui-tokens.mjs`：解析 `src/config/theme.ts` 的 `T`，与原型 `:root` 的 24 个自定义属性逐一 diff → **24/24 一致**。
  - `verify-ui-metrics.mjs`：同时渲染原型(file://)与运营端(:5174 /admin/dashboard)，实测 15 项几何/色值 → **15/15 一致**
    （侧栏 240px、页面底 #f5f2ec、主区 padding 20/24/32 + gap16、KPI 4 列 gap14 圆角10 内距14/16、面板 #fffefb 圆角10 边 1px rgba(74,60,42,.1)、页标题 18px/750、表格行高 46px）。
  - `smoke-console-pages.mjs`（总台 11 页）、`smoke-role-views.mjs`（AGENT/SP/ADMIN 用户视角/USER 拦截 四场景）。
- **修掉的 4 个真实缺陷**：
  1. `authProvider.login` 硬编码 `redirectTo:'/admin/users'` → AGENT/SP 登录后被塞进无权限的用户管理页（连带 403）。改为不返回 redirectTo，交根路由 `HomeRedirect` 按角色分流。
  2. `App.tsx` `/login` 路由的 `<Authenticated>` 分支里 `<Navigate to="/admin/dashboard" replace/>` 硬编码，二次覆盖角色分流 → 换为 `<HomeRedirect />`。**这两处同时存在，只改其一不生效。**
  3. 视角切换 VIEWS 未按角色过滤：SP 切「用户视角」→ `/user/dashboard` → `api/user/*` 403（该控制器 `@Roles('ADMIN','AGENT')`）。新增 `VIEW_VISIBLE` 映射：ADMIN 全可见 / AGENT(agent,user) / SP(provider) / USER(user)。
  4. 原型表格统一 46px 无紧凑变体；运营端 `DataTable dense` 走 40px → 行高改回 `T.row`(46)，`dense` 保留为兼容空属性。
- **补的两处还原缺口**：`index.html` 加 `html,body{background:#f5f2ec}`（原仅内层容器有底色，body 透明）；新增 `public/favicon.svg`（品牌渐变 mark），消除 `/favicon.ico` 404。
- **登录页重构（`pages/Login.tsx`）**：由 antd 默认 Card + #f0f2f5 改为原型暖石底 + 单栏面板 + 品牌渐变 mark + 演示账号说明。
  - 关键坑：Refine `useLogin` 在 authProvider 返回 `{success:false,error}` 时**不抛错、走 onSuccess**，`onError` 不触发（且未配 notificationProvider 所以无 toast）。失败态必须在 `onSuccess(data)` 里判 `data.success===false`。
- **色值收敛**：审计出 35 处脱离 theme.ts 的硬编码色值，全部改为 `T.*` 引用；现仅剩 3 处（DonutShare 色板 2 个外推色 + DataTable 1 处注释）。
- **准入口径确定**：运营端只对 `ADMIN / AGENT / SERVICE_PROVIDER` 开放；`authProvider` 拦截 USER 并提示去用户端。`/user/*` 是 ADMIN/AGENT 的**监督镜像**（配合对象视角选择进入），不是 USER 自助入口。
- **校验结果**：admin `tsc --noEmit` 0 error；`vite build` 成功（2,023.77 kB）；四场景冒烟 **ERRORS 0**、HTTP>=400 全 none。
- **遗留（不阻断）**：dev 下 5 条 antd `useForm is not connected` 告警——弹窗未挂载时 antd 的开发期提示，生产构建无；修复需把弹窗体拆成子组件或加 `forceRender`，后者有表单残留风险，故保留。
