# 2026-09-26

## 账户详情「身份认证资料 + 账号安全」全四层落地（已完成，未提交）

### 后端
- `schema.prisma`：User 新增 `idCardFront` / `idCardBack`（id_card_front / id_card_back，/uploads URL）；迁移 `20260926000000_add_id_card_images` 已 deploy + generate（EPERM rename dll 可忽略，类型已生成）。
- `auth.dto.ts`：UpdateProfileDto 增 idCardFront/idCardBack（≤512）；idCard 收紧为 `^\d{17}[\dXx]$`。
- `auth.service.ts`：新增导出 `isValidIdCard`（GB 11643-1999 mod 11-2）；selectUser 增两影像字段；updateProfile 支持 phone（占用 → **409 ConflictException「手机号已被占用」**）、idCard 非法 → 400、idCardFront/idCardBack；idCard 变更仍自动置 realNameStatus=PENDING。
- `user-console.controller.ts`：resolveTarget select 增 email/avatar/status/idCard/idCardFront/idCardBack/realNameStatus/realNameVerifiedAt/lastLoginAt；dashboard profile 同步输出。
- `admin.service.ts`：userDetailSelect 增 idCardFront/idCardBack（证件号明文仍仅 ADMIN，代理商拿掩码，原逻辑未动）。
- 改 server 源码后已 `build:compiled` + 杀 :3000 由 watchdog 拉起新代码（已验证生效）。

### 运营端 ProfilePage（四层角色通用）
- 新增 `SectionProps`（透传 readOnly/onUpdated）+ `IdentityPanel` + `SecurityPanel`，插在四个角色 Section 的「基础数据」之后。
- IdentityPanel：认证状态 StatusTag（APPROVED 附认证时间）/ 证件号（编辑态 Input+校验位即时报错；只读 maskId；缺失→「待完善」）/ 人像面·国徽面 CertImageField（antd Image preview 预览 + 已上传/未上传 + 编辑态上传，走 /api/assets/upload）。
- SecurityPanel：手机号（本人且 ACTIVE 完整显示，视察/DISABLED 脱敏 + Tooltip）→ PhoneModal（PATCH /api/auth/me）；登录密码 → PasswordModal（POST /api/auth/change-password，旧+新+确认）；微信（wxOpenid 真实判定）/QQ/支付宝 绑定按钮 → `message.info('该功能即将上线')`。
- ProfilePage.save 前置校验：身份证校验位 / 邮箱格式，命中即 message.error 中止。
- AccountDetailSections（ProfileDrawer 复用）传 readOnly，监督视角天然只读。

### web 端 Account.tsx（整页重写）
- 新 Cell 外壳 + CertCell（缩略图点击预览遮罩 + 已上传/未上传 + 上传/重新上传）。
- 编辑态扩为多字段表单（昵称/邮箱/身份证号/证件影像），保存走 authStore.updateProfile（类型已扩 idCard 三字段）后 reload dashboard。
- 手机号修改走 `api.updateProfile({phone})` **不经 authStore**（避免手机号写入 localStorage，沿用 admin 端 PII 纪律）；密码走 store.changePassword。
- Hero/基础数据手机号：本人完整显示，status=DISABLED 才脱敏（与 admin 同口径）。
- UserInfo 增 wxOpenid/idCard/idCardFront/idCardBack 可选字段。

### i18n
- `userCenter.account.*` 新增 50 键（zh-CN + en；ug/kk/ky/uz 走回退）。
- 工具：`apps/admin/e2e/_sync_account_i18n.py`（幂等同步 web locales + admin web-common）。
- ⚠️⚠️ 两个铁律再确认：① i18n 文件读必须 `newline=''`，否则 CRLF→LF 整文件 diff；② admin `web-common.json` 定位对象必须找「以 `{` 结尾的 `"account":` 成员行」——`userCenter` 后第一个 `"account"` 可能是 `menu.account` 字符串（曾误插进 overview）。
- 回归资产：`apps/server/e2e/_smoke_account_security.cjs`（API 冒烟 12 项）、`apps/admin/e2e/_verify_account_security.mjs`（浏览器 18 项，web 存储键 access_token/user_info、admin 是 h5_admin_token/h5_admin_user）。

### 验证
- 三端 typecheck 全绿；API 冒烟 12/12（非法证号 400、合法→PENDING、影像回读、409 占机、改密后新密码可登录）；浏览器 18/18（含截图 shots/_acct_*.png，在 apps/admin/shots/）。
- 测试账号 13900008888 已清理。

### 环境注意
- ★ 并行会话活跃：HEAD 已前进（eb25625 等非本会话提交），admin common.json 有他人未提交改动——**勿对共享文件随手 `git checkout --`**。本轮改动未 commit。

---

## #7 Phase 4：结算总览 / 分账费率（完成）

### 结算总览（admin/settle）
- 路由已存在（`/admin/settle` → `AgentWallet variant="admin"`），本次**增强**而非重建：追加 `useCustom('admin/stats')`（`enabled: isAdmin`）渲染「平台资金总览」Panel（平台总流水 / 平台抽成 / 累计已提现 / 待处理提现），满足「复用 AgentWallet + admin/stats」。
- 前提确认：`agent/wallet` 为 `@Roles('AGENT','ADMIN')`，ADMIN 可调用；`admin/finance` 无 list（纯分组锚点，不渲染，非死链）。

### 分账费率（admin/fee-config）
- 原 `AdminModulePlaceholder` 占位 → 实现真实页 `FeeConfigPage`（consolePages.tsx 末尾）。
- 服务端：新增 Prisma 模型 `PlatformFeeConfig`（全局单例 id='default'）+ 迁移 `20260926010000_add_platform_fee_config`；`GET/PUT /api/admin/fee-config`（@Roles ADMIN）。
- 计算口径（前后端同式）：**providerRate = 100 − platformRate − agentRate**，不接受前端入参，服务端推导落库；校验平台+代理 ≤100、类目费率 0-100 整数；PUT 写 AuditLog（FEE_CONFIG_UPDATE）。
- 前端三区块对齐原占位 sections：平台抽成比 / 服务商分账规则（含分账试算）/ 类目费率（Form.List）。

## 总台（console）菜单与详情页键值中文化校正（完成）
- 侧栏取数：`t('menu.<资源名 /→.>', meta.label)`；分组 `t('group.<key>', GROUP_LABEL[key])`。
- 修正前问题：28 项中 **10 项缺 `menu.*` 键**（回落硬编码 label）；`group.contract` 缺失；en 的 `group.trade/message/personal` **残留中文**；en `menu.admin.qualifications` 误译 "Settlement Review"（应为 Onboarding Approval）；en `profile-provider` 仅 "Account"；既有 `menu.admin.auditLogs` 驼峰键与查找键 `audit-logs` 不匹配（不可达死键）。
- 修正后：menu.* 28/28（zh+en），group 无缺失、en 无残留中文、zh 无错配。
- 逐模块实测 26 条菜单路由 PageHead 标题 vs 菜单文案：修正前 25/26，唯一异常 `/account/profile`（ProfilePage 无标题）→ 补 `PageHead`，标题复用 `menu.account.profile` 与菜单共用真值。修正后 **26/26 一致**。

## 关键坑（高复用价值）
- **本仓库 `prisma migrate dev` 不可用**：shadow DB 重放 `20260924000000_add_team_join_application` 报 `type "MessageScope" does not exist`（既有历史问题）。可用路径：手写 migration.sql → `prisma db execute --file` → `prisma migrate resolve --applied` → `prisma generate`。
- **`prisma generate` 会因服务进程占用 `query_engine-windows.dll.node` 报 EPERM**：须先停看门狗 + `node dist/main.js`，generate 后 `build:compiled`，再重启看门狗。
- 同文件**并行多次 Edit 会 EBUSY/互相覆盖**，必须串行（本次已踩）。
- antd v5 `InputNumber.addonAfter` 已废弃（控制台告警）→ 用 `suffix`。

## 验证
- `pnpm --filter @h5design/{server,admin} run typecheck` 全绿。
- `scripts/smoke-fee-config.mjs` 9 项 ALL PASS；`scripts/smoke-user-zombie.mjs` 13 项 ALL PASS（回归无破坏）。
- `scripts/audit-console-i18n.mjs`（新增，26 模块标题审计）26/26 一致。
- 费率已还原默认（platformRate=10 / agentRate=0 / MONTH）。本轮改动未 commit（遵并行会话纪律）。
