# 2026-09-18 工作日志

## web 端工作台对齐运营端菜单顺序 + 页面内容（架构重构 2.4 收尾）
- 需求：终端用户在 web 与运营端各有一套工作台，要求 web 端（用户中心）按运营端菜单项顺序与页面内容对齐。
- 运营端用户视角菜单真源：`apps/admin/src/config/resources.tsx` 的 `user/*` 资源，四组十一项顺序：
  - 交易中心：我的工作台 / 我的服务商 / 我的订单 / 我的作品
  - 评价与反馈：我的评价 / 我的反馈
  - 消息中心：通知公告 / 业务消息
  - 个人中心：我的钱包 / 优惠与权益 / 账户详情
- 改造 `apps/web/src/user/shared.tsx`：NavItem 增 `group?` 字段；`USER_CENTER_NAV` 重排为上述顺序并打分组标签；新增 `TIERS/tierKeyOf/TIER_PERKS`（与运营端 AdjustUserModal 同源，会员四档权益）。
- 改造 `apps/web/src/user/UserCenterLayout.tsx`：侧栏在分组切换处渲染分组标题（交易中心/评价与反馈/消息中心/个人中心）；移动端 select 改用 optgroup 分组；顺手删掉原先 `out` 分支的重复 Link。
- 改造 `apps/web/src/user/Overview.tsx`（我的工作台）对齐运营端 `UserDashboard`：
  - 资料卡加会员等级 pill + 账号ID(截断)/手机(脱敏)/区域/注册时间（后端 profile 实含 id，已补到 DashboardStats 接口类型）。
  - 面板顺序改为 duo(我的订单 / 会员权益) + trio(关注的服务商 / 我的优惠券 / 我的评价)，与运营端一致；新增「会员权益」卡（按 tier 列 TIER_PERKS）。
  - 订单行补「服务商 + 金额」，优惠券行补「面额」，评价行补「状态」。
- i18n：`common.json`(zh-CN+en) 的 `userCenter.overview` 增 `memberBenefits`/`noRecharge`；其余沿用既有 `account.phone`/`account.region`。
- 验证：`node -e JSON.parse` 两 JSON OK；`pnpm --filter @h5design/web run typecheck` 0 错误。
- 注：userCenter 命名空间此前仅落 zh-CN/en（4 个 RTL/小语种沿用既有缺失口径，未本次补齐）。

## web 端个人中心严格对齐运营端工作台「菜单 + 页面风格」（二期收尾 #68）
- 需求（续上条）：web 端「个人中心」与运营端「工作台」仍有差异，要求严格按运营端菜单与**页面内容/布局样式**对齐。
- 在 `shared.tsx` 镜像运营端用户视角 UI 原语（令牌取自 admin/theme.ts）：`PageHead`/`Panel`/`KpiCard`/`DetailModal`/`FilterBar`/`ReviewEditor`/`UserListPage`/`useUserPage`，及 `serviceRolesText`/`msgTypeText` 标签助手；`DetailFieldDef` 增 `roles` 格式；`UserListPage` 支持 `children`（承载自定义弹窗）。
- 重写 5 个列表/详情页对齐运营端：
  - `Feedback.tsx`：表头 编号/主题/类型/对象/时间/状态 + 胶囊(全部/待回复/已回复) + 搜索 + ＋新增反馈；详情弹窗含回复记录；POST /api/user/complaints。
  - `Providers.tsx`：编号/服务商/服务类型/评分/关注时间/状态 + 搜索 + 详情 + 评价(ReviewEditor,providerId)。
  - `Messages.tsx`：编号/标题/类型/时间/状态 + 胶囊(全部/待处理/已处理) + 详情(标记已读)。
  - `Notices.tsx`：编号/标题/类型/时间/状态 + 胶囊(全部/未读/已读/待补资料) + 详情 + 填写资料(fillKind)。
  - `Account.tsx`：改 `PageHead`+`Panel` 风格；Hero 资料卡(朱砂→深赭渐变头像 + 角色 pill) + 四 Panel（基础数据/归属与作用域/账户状态/会员与资产），对齐运营端 `ProfilePage` 的 `UserSections` 布局；昵称内联编辑保留。
- `UserCenterLayout.tsx` 侧栏改用主题令牌：`bg-[#fffefb]`+`border-[rgba(74,60,42,0.10)]`（去阴影，B 端靠线不靠阴影）、激活 `bg-[#c24b2e] text-white`、分组标题 `text-[#86765f]`。
- i18n：userCenter 增 `account.*`(基础数据/归属与作用域/账户状态/会员与资产 等)、`search.*`、`filters.*`、`ticketType.*`、`status.*`、`feedback.*`、`notices.fill` 等；zh-CN+en 同步。
- 后端字段已核对：`providers` 含 rating/serviceRoles/followedAt；`reviews` 含 orderNo/serviceName/status:'已发布'；`complaints` 含 target/code；`messages/notices` 状态为 '已读'/'未读'/'初审通过'。
- 修复：列表页 `fetcher` 箭头表达式体误加尾逗号导致 TS1109；`UserListPage` 未解构 `children` 导致 TS2304。
- 验证：`pnpm --filter @h5design/web run typecheck` 0 错误；两 JSON `JSON.parse` OK。

## 「我的作品」嵌入个人中心左侧菜单右侧区域（#works-in-layout）
- 需求：web 端点击左侧「我的作品」后跳转的页面丢失了左侧个人中心侧边栏，要求把该页套进个人中心右栏（沿用其它菜单项的操作布局）。
- 根因：`/dashboard`（我的作品）原本是 `App.tsx` 中挂在 `AppLayout` 下、`UserCenterLayout` 之外的顶层路由，故无左侧菜单。
- 改造：
  - `App.tsx`：新增嵌套路由 `/user/works`（在 `UserCenterLayout` 内部渲染 `ProjectList`）；原 `/dashboard` 顶层路由改为 `<Navigate to="/user/works" replace />`（保留深链/书签兼容）。
  - `shared.tsx`：`USER_CENTER_NAV` 的「我的作品」项 `to` 由 `/dashboard`(out:true) 改为 `/user/works`（去掉 out）；同步更新 NavItem 注释。
  - 全站 `/dashboard` 引用统一改为 `/user/works`：`SiteHeader.tsx`(账号菜单 + 顶栏按钮)、`Register.tsx`(注册后跳转)、`Login.tsx`(默认回跳)、`Editor.tsx`(返回列表)。
  - `ProjectList.tsx`：去掉外层 `max-w-7xl`/双重 `bg-white` 与内层 `<main>`（避免 `<main>` 嵌套）；改用与个人中心一致的「PageHead 风格标题 + 副标题」+ 胶囊筛选(全部/作品/模板，激活朱砂实心) + 朱砂新建按钮；指标卡改为暖色令牌(`bg-[#fffefb]`+`border-[rgba(74,60,42,0.10)]`+深赭字)；移除原 `SiteFooter`/`ProviderSection`（与其他菜单项一致，个人中心内不再重复全站页脚）。
  - i18n：zh-CN/en 的 `userCenter.menu` 增 `worksSubtitle`（管理你的 H5 作品与模板 / Manage your H5 works and templates）。
- 验证：`pnpm --filter @h5design/web run typecheck` 0 错误；两 JSON `JSON.parse` OK；确认全仓仅剩 `/dashboard` 重定向引用。

## web 个人中心 UI 像素级对齐运营端（令牌镜像收尾）
- 需求：菜单与内容已对齐，但 UI 视觉（布局/间距/配色/字号/圆角/状态色/头像/侧栏形态）仍需严格按运营端 antd/Refine 原生组件落地。采用令牌镜像策略（不把 antd 装进 web），以 `apps/admin/src/config/theme.ts` 为真值。
- 改动：
  1. `shared.tsx`：PageHead h2 `font-[750]`、Panel header `font-[650]`；StatusBadge `px-[9px] py-0.5`；KpiCard 数值去掉 `font-mono` 改内联 `Bahnschrift/DIN` + `text-[27px] font-[750] tracking-[.01em]`（对齐 admin Statistic）；UserTable 单元格 `text-[#2a2118]`(ink1)；FilterBar 容器 `px-3 py-2.5`、Pager 按钮 `px-2.5 py-[3px]`；ReviewEditor scrim `rgba(30,24,16,.45)`+底色 `#fffefb`+`rounded-[14px]`；Stars 改朱砂主色数值字。
  2. `UserCenterLayout.tsx`：侧栏 `w-60`(240)+ 品牌区(34×34 渐变 logo + 庆柬云/个人中心)+ 分组标题栏(暖石底+3px 朱砂竖条+13.5px/700)+ 选中项朱砂软底+3px 竖条+18px 图标；主区 `space-y-4 px-0 pb-8 pt-0`。
     - **修复**：外层 wrapper `<div>` 漏闭合导致 TS17008/TS1381，补 `</div>` 后通过。
  3. `Overview.tsx`：资料卡改 `.ucard` 暖白描边卡(46px 渐变头像 + tier Pill + 信息行)；会员权益逐项 chip 化。
  4. `Account.tsx`：Field 加 `num` 右对齐等宽数值；Hero `px-5 py-[18px]`+64px 渐变头像+名称 18px。
  5. `ProjectList.tsx`：来源筛选胶囊对齐 FilterBar 芯片（选中 `bg-[rgba(194,75,46,0.10)] font-semibold text-[#c24b2e]`，原实色填充）。
- 验证：`pnpm --filter @h5design/web run typecheck` 0 错误。
- 其余列表页（Orders/Wallet/Coupons/Reviews/Feedback/Providers/Messages/Notices）复用共享原语，随原语对齐自动受益。

## web 个人中心细节复核 + 优化对齐（用户反馈：折叠/字号/字段名三处差异）
- 用户反馈：UI 已基本一致，但①侧边栏分组名称不可折叠/展开；②页面字体大小不一致；③个别字段名称不一致。要求「完全按运营端样式落地」并仔细复核。
- ① 侧边栏分组折叠：`UserCenterLayout.tsx` 已落地（前序 session）：按 `group` 聚合渲染 `<button>` 分组头（朱砂竖条 + 旋转箭头 SVG）+ `localStorage('uc.sider.fold')` 持久化 + 命中当前路由自动展开。**修复**：该文件 import 漏带 `useMemo/useEffect/useState`（导致 TS2304 + 隐式 any 级联），补齐后 web typecheck 0 错误。
- ② 字体对齐：运营端真值 `profile.css` 标签 `11.5px/700/uppercase`、值 `13.5px`（ink1）；web `Account.tsx` 原 `Field` 统一 `text-sm`(14px)。改为 label `text-[11.5px] font-bold`、value `text-[13.5px]`，数值类去 `font-mono` 改 Bahnschrift/DIN（`tabular-nums`）；昵称内联块同步 11.5/13.5。
- ③ 字段名/字段集对齐（以 admin `ProfilePage.UserSections` 为唯一真值）：
  - `region`「所在地区」→「所在城市」；i18n zh-CN/en `userCenter.account.region` 同步。
  - `pendingReview`「待评价订单」→「进行中订单」，且**纠正数据源**：原 web 用 `pendingReviewOrders`（已支付未评价），运营端 `activeOrders`=近 30 天已支付订单；改为新增 `activeOrders` 字段（口径对齐 admin /auth/me），i18n 键由 `pendingReview` 更名 `activeOrders`（原 account 内 `pendingReview` 键弃用）。
  - 补「最后登录」(`lastLoginAt`，格式 `toLocaleString hour12:false`) 与「关注服务商」(`followingProviderCount`) 两个缺失字段。
- **顺带修复数据读取 bug**：`/api/user/dashboard`（`user-console.controller.ts` `@Controller('api/user')`）把资料字段嵌套在 `profile` 下，而 `Account.tsx` 此前在顶层 `p` 上读取 → 姓名/昵称/手机号/城市/注册时间/ID 全为空。新增 `const prof = p.profile ?? {};` 统一从 `prof` 读取；Hero 信息行与昵称编辑初始值一并修正。
- 后端改动 `user-console.controller.ts`：`me` 查询 `select` 增 `lastLoginAt`/`followingProviderCount`；事务增 `activeOrders`（近 30 天 paid 计数）；return 透出三字段。
- i18n：`zh-CN`/`en` 的 `userCenter.account` 块改 `region`/`activeOrders`，增 `followingProviders`/`lastLogin`；4 个 RTL/小语种无 userCenter 块，依赖 `fallbackLng:'en'` 兜底（未补齐，沿用历史口径）。
- 验证：`pnpm --filter @h5design/web run typecheck` 0 错误；`pnpm --filter @h5design/server exec tsc --noEmit` 0 错误；两 JSON `JSON.parse` OK。
