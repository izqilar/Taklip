# 2026-08-30 工作日志

## 经营总览「成交订单」看板改造（四台统一）
- 背景：用户指出原"成交订单"KPI 卡把"全量订单数"误当"成交订单"展示，且 foot 的"退款率"实为退单率（笔数口径）；要求按电商/财务口径拆成 订单总数/成交订单数/成交率/退单数/退单率/退款率。
- 已对齐意见：退款率=金额口径(退款金额/总支付金额)、布局=KPI 卡(成交订单数主数字+foot 成交率/退单率)+下方「成交订单构成」2×3 面板、四台统一、平台总流水改净额。
- 后端（apps/server/src/console/*）：四个 dashboard 接口均新增 orderTotal/dealOrders(paid)/refundedOrders(refunded)/dealRate/returnRate/refundRate；总台与代理 totalRevenueCents+platformFeeCents 改净额(扣退款)，服务商月收入 monthlyRevenue 同步净额冲减，用户 totalSpentCents 本就为净额。
- 前端：新增 `components/ui/DealOrdersPanel.tsx`（六指标 2×3 网格，成交/退单用 up/down 色），接入 总台 dashboard.tsx、代理 AgentDashboard、服务商 SPStudio、用户 UserDashboard。
- 校验：admin tsc 0 error；nest build 通过；重启 3000/5174；ADMIN 登录核对四台字段——总台 92/82/10、成交率 89.1%、退单率 10.9%、退款率 9.7%(金额)、流水净额 ¥6578.00；Playwright 四台均渲染"成交订单构成"且六指标齐全、无控制台报错。截图 temp/console-deal.png。
- 口径说明：订单 TemplateOrder.status 仅 paid/refunded 两种，故 订单总数=成交+退单；后续若加"已支付未交付/已取消"状态可再细化"成交"定义。

## 运营端「我的资料」详情页（三区块 + 编辑状态机）
- 决策（用户拍板）：实名认证=用户自助填证件号提交后走 PENDING 待管理员裁定；下拉移除 operator 的"查看我的资料"文字入口(只留编辑+退出)；locale 放开编辑(补 DTO)；手机/微信保持只读；subject 监督对象本轮只保持只读抽屉。
- 后端：schema.prisma 新增 `RealNameStatus` 枚举(UNVERIFIED/PENDING/APPROVED/REJECTED) + User 字段 idCard/realNameStatus/realNameVerifiedAt。`UpdateProfileDto` 加 idCard/locale；`updateProfile` 提交 idCard 即置 PENDING 并清 verifiedAt；`selectUser` 扩展 providerWallet/region/agent/_count/wxOpenid；新增 `GET /api/auth/me`(getMe) 供详情页拉全量自资料。nest build + prisma generate(db push) 通过。
- 前端：LayerHeader 下拉移除 operator"查看我的资料"、保留 subject"查看对象资料"、新增「编辑资料」(跳 /account/profile?mode=edit)；LayerSider 顶部新增常驻「我的资料」菜单(IdcardOutlined)；新建 `pages/account/ProfilePage.tsx`：三区块(基础资料可编辑/角色与归属+实名认证只读/关联业务只读)+编辑状态机(编辑→保存/取消，只发改动字段 PATCH /me，成功写回 getStoredUser 让胶囊刷新)；头像用 POST /api/assets/upload。
- BUG：跳同路由 ?mode=edit 时组件已挂载、mount 效应不重跑→编辑态不激活；改为监听 useSearchParams 变化 + 取消时 setParams({})。
- 校验：admin tsc 0 error；Playwright 14/14 PASS(侧栏菜单/三区块/编辑保存/实名待审/下拉编辑/SP 资质状态/控制台 0 报错)。截图 temp/verify-profile-admin.png。验证后已 prisma 回滚 dev 管理员昵称/证件/状态，避免测试污染。
- 沉淀：`console-account-module` skill 扩写「我的资料」详情页范式（三区块 + 编辑状态机 + 实名认证 PENDING + GET /api/auth/me + 下拉编辑资料跳 ?mode=edit + LayerSider 常驻菜单 + ?mode=edit 跳转 bug 修法 + 测试污染回滚）。

## 运营端「个人资料」菜单与布局重排
- 用户要求：侧边栏「我的资料」移到「系统」分组下、位于「角色与权限」下方，并改名为「个人资料」；顶部胶囊「编辑资料」改为「个人资料」并跳到侧边栏个人资料页；详情页三区块横向宽松布局。
- 改动：
  - `apps/admin/src/config/resources.tsx`：控制台/代理商/服务商三层均注册 `list: '/account/profile'` + `group: '系统'` 的「个人资料」菜单，紧跟同层「角色与权限」；`agent/roles`、`sp/roles` 也加入 `group: '系统'` 使分组一致。
  - `apps/admin/src/components/layout/LayerSider.tsx`：删除顶部硬编码「我的资料」块及其 `IdcardOutlined` import，菜单完全由 resources 驱动。
  - `apps/admin/src/components/layout/LayerHeader.tsx`：胶囊下拉「编辑资料」→「个人资料」，`navigate('/account/profile')`；抽屉默认标题同步为「个人资料」。
  - `apps/admin/src/pages/account/ProfilePage.tsx`：标题「个人资料」；三区块从垂直堆叠改为 CSS Grid `repeat(3, minmax(0, 1fr))`，卡片 `T.bg` 面板底 + `T.rMd` 圆角 + 等宽同高；为卡片加 `className="profile-block"`；Descriptions label 宽度统一 96px 以适应窄卡片。
- 校验：admin `tsc --noEmit` 0 error；Playwright smoke(`temp/smoke-profile-relayout.cjs`) 验证：页标题「个人资料」、侧栏顺序「角色与权限/个人资料/系统设置」、三卡片横向等宽高、下拉含「个人资料」不含旧「编辑资料」、控制台 0 报错。`temp/verify-profile.cjs` 与 `temp/verify-account-module.cjs` 同步更新命名后，后者 15/15 PASS，前者 13/14（剩余 1 项为既有 antd `message` static function warning，非本次引入）。
- 测试污染回滚：运行 `apps/server/temp/reset-admin-profile.mjs`（Prisma 直连 `../prisma/prisma-client`），将 dev 管理员 13800000002 的 nickname/idCard/realNameStatus 恢复为开发管理员/空/UNVERIFIED。
- 沉淀：更新 `console-account-module` skill 中的命名、资源注册方式、横向三列布局、Playwright 断言要点。

## 「个人资料」→「账户详情」全量重构（四角色四分区 + 原型还原 + 数据库落地）
- 用户要求（5 项）：①侧栏「个人资料」改名「账户详情」；②按 `docs/四类角色个人资料字段规范.md` + `UI_Design/index.html` 的「账户详情」页重做四角色内容与布局；③补 USER 视角；④布局样式贴近原型（附 8 张截图，但当前模型不支持读图，故直接解析 index.html 的 HTML/CSS 还原）；⑤数据库按规范落地。
- 改名：`resources.tsx` 三处（console/agent/provider）label →「账户详情」；`LayerHeader.openProfile` 标题 `userId ? '用户资料' : '账户详情'`，胶囊项同步。
- 布局（对齐原型）：Hero（64px 渐变头像 + 姓名 + 角色 tag + `ID·手机·区域·注册日期` + 编辑/保存/取消）→ 4 个全宽 panel 纵向堆叠（gap 14）→ 每 panel 内 `grid repeat(2,1fr)` 字段网格（`.pfield`：label 11.5px uppercase 弱色 / val 13.5px，仅奇数列带右边框）。
- 四角色第四区块：ADMIN 职能与权限（+ `.perm-tags .pt` 权限标签云，14 项）/ AGENT 辖区经营（月流水·抽成10%·用户数·服务商数·本月订单·待初审）/ SP 服务与资质 / **USER 会员与资产（新增）**（会员等级·积分·钱包余额·累计消费·进行中订单·关注服务商）。
- 新增 `apps/admin/src/styles/profile.css`（运营端无全局 CSS）：颜色全走 CSS 变量，由 `profileCssVars` 从 `theme.ts` 的 `T.*` 注入，禁止在 CSS 里硬编码；`panel-title`/`pfield`/`perm-tags .pt` 同时作为 E2E 锚点。
- 抽屉统一：废弃 ProfileDrawer 旧的 antd `Descriptions` 竖排，改为复用 `AccountDetailSections`，与「账户详情」页同布局；抽屉宽度 560。
- 后端：Prisma 新增 User `lastLoginAt/points/totalSpent/userBalance/followingProviderCount` + ProviderWallet `frozen`（金额均为分，展示 /100）；`login()`/`refresh()` 写 lastLoginAt；**`buildRoleExtra(user)` 抽成公共方法**，`getMe()` 与 `AdminService.getUserById()` 共用（ADMIN 权限标签 / AGENT 辖区聚合 / SP 在售与收益 / USER vipTier+activeOrders）；`admin.service` 新增 `userDetailSelect`（含 idCard 等敏感字段，仅详情返回不进列表）。
- 数据落地：seed-test-data 补 USER 的 points/totalSpent/userBalance/following/vipLevel + 钱包 frozen；`temp/patch-dev-profiles.mjs`（补 lastLoginAt）、`temp/patch-dev-wallet.mjs`（dev SP 钱包 124000/32000/68800/0）。
- 修复的真实缺陷：`selectUser` 漏 `createdAt` → 页面「注册/入驻时间」显示「—」（已补，此前 API 校验 ALL_OK 才暴露）。
- 校验：**API 5/5 ALL_OK**（`temp/verify-account-detail-api.mjs`，四角色 + subject 分支，无缺字段）；**浏览器三角色冒烟全过**（`temp/smoke-account-details.cjs`：侧栏顺序 角色与权限<账户详情<系统设置、无旧名残留、4 个 panel-title 齐全、`.pfield` 18~20、admin `.perm-tags .pt` 14、console 0 报错，截图 `temp/account-details-{admin,agent,sp}.png`）；**USER 视角抽屉冒烟通过**（`temp/smoke-user-drawer.cjs`：四分区含「会员与资产」，20 字段，会员与资产=普通用户/860/¥320/¥2,680/4/2，截图 `temp/account-details-user-drawer.png`）。admin `tsc --noEmit` 0 error；server `nest build` 0 error。
- 沉淀：重写 `console-account-module` skill（更名、四分区布局表、profile.css 与 CSS 变量、buildRoleExtra、userDetailSelect、导出 AccountDetailSections、构建命令速查），并新增 12 条常见坑。

### 补充：原型硬指标客观校验（同日追加）
- 新增 `apps/admin/temp/verify-account-details-metrics.cjs`：把同屏实测 computed style 与 `UI_Design/index.html` 的 CSS 逐项比对，覆盖 Hero padding/头像尺寸字号/姓名字号/meta/分区 gap/网格列数/`.pfield` padding 与奇偶边框/label/val/面板头/hint/权限标签共 **34 项**。
- 首轮 28/34，据此修正 4 处实现偏差：Hero padding `16px 18px`→`18px 20px`、头像 fontSize 22→26 且 fontWeight 600→700、分区间距 18→14、`.perm-tags` padding `12px 16px 16px`→`14px 16px 4px`；另 6 项失败是脚本选择器问题（Panel 用 `S.panel` 内联样式，DOM 无 `.panel` 类），已给组件补 `.profile-panel`/`.panel-head`/`.panel-hint` 类名并修正选择器。
- 最终 **34/34 全通过**；同时清掉 ProfilePage 里未使用的 `formatCents` import。
- 最终回归全绿：API 5/5 ALL_OK、三角色冒烟（侧栏顺序/无旧名/四分区/标签14/字段18~20/console 0 报错）、USER 抽屉四分区 20 字段、admin tsc 0 error、server nest build 0 error。
