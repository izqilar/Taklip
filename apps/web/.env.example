# 局域网 / 公网一键切换模板（本文件入库，clone 后照抄为 .env.local 再改）
# 启动 dev 时 Vite 会自动读取同目录的 .env.local（若存在优先于本文件）。
# 不要在本文件里填真实 IP / 域名——本文件入库，改自己的 .env.local。

# —— 二维码 / 分享链接基址（手机微信扫码打开的就是它）——
# 局域网调试：改成你电脑的局域网 IP，例如 http://192.168.1.20:5173
# 生产公网：改成 https://你的 web 公网域名
VITE_WEB_BASE=http://192.168.x.x:5173

# —— dev server 监听网卡（可选）——
# 留空 = 监听所有网卡（配置默认 true，已放开 LAN，手机/同网段可直连）
# 仅本机访问时设回 127.0.0.1：
# VITE_DEV_HOST=127.0.0.1
