<h1 seq="auto">交互规则</h1>
<h2 seq="auto">审核流程</h2>
<p>状态机：待审核 → 已通过 / 已驳回。驳回必填原因；通过写入通过意见。代理商辖区服务商审核含初审 → 待复审 → 已通过，业务升级审核独立成链（服务商业务类型可为多个项目，升级需过代理商审核关卡）。财务提现审核由财务管理员全权处理。</p>
<h2 seq="auto">编辑保存</h2>
<p>查看态显示「编辑」；进入编辑态按钮变为「保存」，同时显示「取消」；保存后权限组合 / 字段写入数据并刷新列表，随后返回查看态。编辑框预填当前记录，杜绝空白表单。</p>
<h2 seq="auto">搜索 / 筛选 / 导出</h2>
<ul>
<li>分类筛选：全部 / 各状态为可点击按钮，点击即检索对应列表，去计数只留名称。</li>
<li>关键词搜索：输入框 + 独立「搜索」按钮 + 回车触发；输入内容后悬停显示清除叉，点击一键清空。</li>
<li>导出：列表数据导出为 CSV（受「数据导出」权限点控制）。</li>
<li>对象搜索：视角内按 ID / 用户名 / 昵称 / 手机号 / 区域模糊检索并选择具体对象，整页联动。</li>
</ul>
<h2 seq="auto">只读锁定</h2>
<p>运维管理员默认只读：黄色提示条常驻，主操作（新建 / 通过 / 驳回 / 编辑 / 调整用户数据）全部禁用，仅保留查看与导出。</p>
<h2 seq="auto">待办直达</h2>
<p>首页待办卡片（入驻审核 / 提现 / 评价等）点击直达对应列表页；侧栏角标实时反映待办计数。</p>

<h1 seq="auto">设计系统</h1>
<h2 seq="auto">色彩</h2>
<table>
<thead><tr><th><p>用途</p></th><th><p>取值</p></th></tr></thead>
<tbody>
<tr><td><p>强调主色（朱砂）</p></td><td><p>#c24b2e（品牌名 / 主按钮 / KPI 高亮）</p></td></tr>
<tr><td><p>副色（青）</p></td><td><p>#14676b（次级强调 / 数值）</p></td></tr>
<tr><td><p>底色</p></td><td><p>暖石微染 #faf5eb 系（背景 / 卡片 / 对象工具栏）</p></td></tr>
<tr><td><p>成功 / 警示 / 危险</p></td><td><p>绿 / 琥珀 / 品红（状态标签语义）</p></td></tr>
<tr><td><p>返回总台按钮</p></td><td><p>绿色（三视角页顶部，右缘对齐角色标签）</p></td></tr>
</tbody>
</table>
<h2 seq="auto">字阶 / 圆角 / 动效</h2>
<p>KPI 数字 27px、页题 18px、正文 14px、表头 12.5px；圆角：卡片 10 / 控件 6 / 胶囊 999；动效统一 180ms；按钮、输入框、标签、表格、弹窗、下拉均为统一组件样式。</p>
<h2 seq="auto">状态与异常四态</h2>
<ul>
<li>加载态：骨架 / 加载中占位。</li>
<li>空状态：无数据时展示引导空态。</li>
<li>异常态：接口失败展示错误提示与重试。</li>
<li>只读态：顶部黄色提示条 + 主操作禁用。</li>
</ul>

<h1 seq="auto">后端开发建议</h1>
<h2 seq="auto">接口与数据模型落地</h2>
<ul>
<li>四层登录：登录接口返回账号角色与所属层级；前端据此渲染对应菜单与作用域。</li>
<li>列表接口：page key → 后端返回标题 / 列定义 / 行数据 / 筛选与计数；搜索、筛选、导出为公共参数。</li>
<li>审核接口：POST 通过 / 驳回（驳回原因必填），服务端做状态机校验与幂等。</li>
<li>对象视角接口：按代理商 / 服务商 / 用户 ID 返回该对象整页聚合数据（KPI + 列表）。</li>
<li>角色权限接口：角色 CRUD + 权限点数组存取；不同层级角色数据相互隔离。</li>
</ul>
<h2 seq="auto">权限控制落地</h2>
<p>RBAC + 位阶校验：功能权限按 10 域 28 点配置到角色；接口层拦截「数据作用域 × 功能权限 × 位阶」三维校验；超级管理员最高，运维管理员只读 + 授权写，服务审核员仅审核域，财务管理员仅财务域，在线客服仅查询回复；自定义角色由各层负责人按管辖范围创建并限定在其数据域内。</p>
<h2 seq="auto">数据隔离落地</h2>
<p>区域表含 regionPath（如 乌鲁木齐 / 天山区）；代理商绑定辖区；服务商挂所属区域与服务类型；订单 / 反馈 / 消息关联主体。后端以当前登录主体推导可见域，任何接口不可越层取数。</p>
<h2 seq="auto">验收清单</h2>
<ol>
<li>四层登录与菜单投影正确，总台 13 项可见全量，低层无高层入口。</li>
<li>视角切换后侧栏 / 面包屑 / 首页 / 数据作用域整体联动；三视角「返回总台」按钮右缘对齐、绿色。</li>
<li>对象视角可按 ID / 用户名 / 昵称 / 手机号 / 区域检索，整页数据联动到所选对象。</li>
<li>超级管理员可写可代操作；运维管理员只读，黄色提示条 + 操作禁用。</li>
<li>角色与权限：总台管理员展开 5 职能角色且位阶正确；功能权限复选框按层过滤（代理 9 域 / 服务商 6 域）；编辑可保存权限组合。</li>
<li>服务商入驻审核进总台审核队列；代理商辖区服务商支持初审 / 复审 / 业务升级审核。</li>
<li>各页分类筛选按钮可点击并检索对应列表；搜索框支持清空叉；导出可用。</li>
<li>详情对话框按页面业务定制（A 查看 / B 审核 / C 编辑 / D 仅编辑），驳回原因必填，编辑预填不空白。</li>
<li>状态字段全部中文化，无英文键值；列表与对话框状态一致。</li>
<li>四态（加载 / 空 / 异常 / 只读）齐全。</li>
</ol>
<h2 seq="auto">未决事项</h2>
<p>真实鉴权体系（JWT / Session）与密码策略；数据库 DDL 与迁移；消息推送通道（短信 / IM / 推送）；支付与资金账户对接；多语言（维汉双语）支持范围。以上未决项不影响本文档对应 UI 契约的实现，由后端技术选型阶段确认。</p>
