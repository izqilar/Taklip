<title>庆柬云 · 分层管理后台 开发文档</title>

<blockquote><p>版本：v1.0（2026-08-28）｜依据：已落地可运行原型 index.html（单文件）｜状态：方向已定，本文档为开发契约</p></blockquote>

<h1 seq="auto">文档说明</h1>
<p>本文档由已通过验收的分层管理后台 UI 原型提炼而成，作为后端与前端开发团队实现「庆柬云」B 端管理系统的完整契约。读者：产品负责人、前后端工程师。范围：总体架构、角色权限、菜单与页面规格、详情对话框、数据模型、交互规则、设计系统与后端落地建议。</p>
<p>术语：总台（ADMIN 管理总台）、代理商（AGENT）、服务商（SP）、用户（USER）；「视角」指管理员在总台侧通过页头切换进入某一角色层的工作界面；「对象视角」指在某角色层内检索并切换到某一个具体代理商 / 服务商 / 用户，查看其名下数据。</p>

<h1 seq="auto">项目概述</h1>
<h2 seq="auto">背景与目标</h2>
<p>平台采用「一个后端 · 多角色分层登录」模式：管理总台可全盘管理所有菜单与数据；代理商仅见代理商中心；服务商仅见服务商中心；用户视角用于运营对用户数据的观察、调整与会员等级功能测试。目标：一套后端代码承载四个决策层级，菜单与数据按层级投影，权限不越层。</p>
<h2 seq="auto">设计原则</h2>
<ol>
<li>一个后端、分层治理、总台统筹：登录即定层，菜单 = 该层作用域投影，低层看不到高层入口。</li>
<li>总台 13 项超集 + 页头视角切换：总台侧边栏只放治理菜单，通过「总台 / 代理商 / 服务商 / 用户」视角切换进入各层工作界面。</li>
<li>可写 / 只读按管理员账号：超级管理员可写（总台可代操作）；运维管理员默认只读，单独授权可写。</li>
<li>服务商入驻审核进总台审核队列（admin/providers），总台统一把关。</li>
<li>底层角色权限不得触达高层权限范围：各层仅显示本层权限域。</li>
</ol>
<h2 seq="auto">非目标</h2>
<p>本文档不包含移动端 C 端设计、支付通道实现、消息推送通道细节；不包含真实鉴权与数据库 DDL（见后端开发建议章节的接口约定）。</p>

<h1 seq="auto">总体架构</h1>
<h2 seq="auto">分层登录模型</h2>
<table>
<thead><tr><th><p>层</p></th><th><p>菜单数</p></th><th><p>数据作用域</p></th><th><p>首页</p></th></tr></thead>
<tbody>
<tr><td><p>管理总台 ADMIN</p></td><td><p>13（超集）</p></td><td><p>ALL（全平台）</p></td><td><p>经营总览</p></td></tr>
<tr><td><p>代理商中心 AGENT</p></td><td><p>8</p></td><td><p>REGION（本辖区）</p></td><td><p>辖区概览</p></td></tr>
<tr><td><p>服务商中心 SP</p></td><td><p>8</p></td><td><p>SELF（自身）</p></td><td><p>我的工作台</p></td></tr>
<tr><td><p>用户视角 USER</p></td><td><p>7</p></td><td><p>SELF（单用户）</p></td><td><p>用户工作台</p></td></tr>
</tbody>
</table>
<h2 seq="auto">视角切换机制</h2>
<p>页头提供「总台视角 / 代理商视角 / 服务商视角 / 用户视角」切换控件（segmented）。切换后侧栏菜单、面包屑、首页、数据作用域、页题整体联动；三层各有独立首页，登录 / 切换后自动落位。</p>
<h2 seq="auto">可写 / 只读模型</h2>
<p>页头账号菜单可在「超级管理员（可写 · 可代操作）」与「运维管理员（只读 · 默认只读，单独授权可写）」间切换。只读时：顶部出现黄色提示条、主操作按钮禁用、行内通过 / 驳回 / 编辑锁定；只读态可查看全部数据。</p>
<h2 seq="auto">数据隔离</h2>
<p>作用域隔离由后端 <code>regionPath / 所属服务商</code> 强制：总台见全量；代理商仅见其辖区（regionPath 前缀）；服务商仅见自身服务域；用户视角仅见当前选中用户。原型为前端演示数据，真实实现由后端按当前角色与登录主体返回。</p>

<h1 seq="auto">角色与权限体系</h1>
<h2 seq="auto">角色类型</h2>
<p>总台管理员展开为 5 个职能角色，权限位阶从高到低：超级管理员 → 运维管理员 → 服务审核员 = 财务管理员 → 在线客服。其他决策层可在其管辖范围内设立同类职能角色，并支持创建自定义角色。</p>
<ul>
<li>超级管理员：所有内容可读可写，可创建 / 授权角色。</li>
<li>运维管理员：所有内容默认可读，部分内容可写（开通权限）。</li>
<li>服务审核员：只读 + 处理总台审核类内容（入驻 / 模板 / 公告 / 申诉）。</li>
<li>财务管理员：全权处理提现等财务内容（提现 / 钱包 / 流水 / 对账）。</li>
<li>在线客服：答复各类问题，除查询回复外无实质业务权限。</li>
</ul>
<h2 seq="auto">权限三要素</h2>
<p>每条角色记录含：归属层级（总台 / 代理商 / 服务商）× 类型（内置 / 自定义）× 职能，叠加权限三要素：数据作用域（ALL / REGION / SELF）、功能权限（权限点勾选集合）、授权边界（数据 / 功能 / 位阶）。</p>
<h2 seq="auto">功能权限点（10 个功能域 · 28 个权限点）</h2>
<table>
<thead><tr><th><p>#</p></th><th><p>功能域</p></th><th><p>权限点（复选框）</p></th></tr></thead>
<tbody>
<tr><td><p>①</p></td><td><p>用户与账号</p></td><td><p>用户查询 / 用户编辑（角色·状态·启停）</p></td></tr>
<tr><td><p>②</p></td><td><p>入驻与服务商</p></td><td><p>入驻审核 / 资质审核 / 业务升级审核</p></td></tr>
<tr><td><p>③</p></td><td><p>服务与内容</p></td><td><p>模板/服务上架 / 内容下架 / 模板审核 / 资质管理</p></td></tr>
<tr><td><p>④</p></td><td><p>订单与履约</p></td><td><p>订单查询 / 订单处理（接单·履约·交付）/ 售后处理</p></td></tr>
<tr><td><p>⑤</p></td><td><p>财务</p></td><td><p>流水查询 / 提现审核 / 提现代操作 / 对账</p></td></tr>
<tr><td><p>⑥</p></td><td><p>优惠券与权益</p></td><td><p>券/权益配置 / 优惠券发放 / 权益查询</p></td></tr>
<tr><td><p>⑦</p></td><td><p>评价与申诉</p></td><td><p>反馈处理 / 申诉仲裁</p></td></tr>
<tr><td><p>⑧</p></td><td><p>消息与公告</p></td><td><p>公告发布 / 公告审核 / 站内信收发</p></td></tr>
<tr><td><p>⑨</p></td><td><p>组织与系统</p></td><td><p>角色管理（创建/授权）/ 区域·代理商管理 / 系统设置</p></td></tr>
<tr><td><p>⑩</p></td><td><p>数据导出</p></td><td><p>列表数据导出（用户/订单/流水/评价等全列表通用）</p></td></tr>
</tbody>
</table>
<h2 seq="auto">三层功能域映射</h2>
<table>
<thead><tr><th><p>层级</p></th><th><p>展示功能域</p></th><th><p>隐藏</p></th></tr></thead>
<tbody>
<tr><td><p>总台</p></td><td><p>①~⑩ 全 10 域</p></td><td><p>—</p></td></tr>
<tr><td><p>代理商</p></td><td><p>①②④⑤⑥⑦⑧⑨⑩</p></td><td><p>③ 服务与内容</p></td></tr>
<tr><td><p>服务商</p></td><td><p>③④⑤⑦⑧⑩</p></td><td><p>①②⑥⑨</p></td></tr>
</tbody>
</table>
<h2 seq="auto">角色-权限映射示例（总台）</h2>
<table>
<thead><tr><th><p>角色</p></th><th><p>勾选权限点</p></th></tr></thead>
<tbody>
<tr><td><p>超级管理员</p></td><td><p>28 项全勾</p></td></tr>
<tr><td><p>运维管理员</p></td><td><p>用户查询、订单查询、流水查询、权益查询、反馈处理、列表数据导出</p></td></tr>
<tr><td><p>服务审核员</p></td><td><p>入驻审核、资质审核、业务升级审核、模板审核、反馈处理、申诉仲裁、公告审核、列表数据导出</p></td></tr>
<tr><td><p>财务管理员</p></td><td><p>流水查询、提现审核、提现代操作、对账、权益查询、列表数据导出</p></td></tr>
<tr><td><p>在线客服</p></td><td><p>用户查询、订单查询、反馈处理、站内信收发</p></td></tr>
</tbody>
</table>

<h1 seq="auto">界面框架</h1>
<h2 seq="auto">整体布局</h2>
<p>三段式：顶部页头（品牌 + 视角切换 + 对象选择入口 + 账号模式）｜左侧侧栏（按当前视角渲染菜单，含分组与角标）｜主内容区（面包屑 + 页题 + 筛选/搜索/导出工具条 + 数据表 + 分页）。</p>
<h2 seq="auto">页头要素</h2>
<ul>
<li>品牌「庆柬云」；当前视角 crumb（管理总台 / 代理商中心 / 服务商中心 / 用户视角）。</li>
<li>视角切换 segmented；账号菜单（超级管理员 / 运维管理员，可写 / 只读标签）。</li>
<li>代理商 / 服务商 / 用户视角页顶部含「返回总台」按钮，右缘对齐页面 pghead 内角色标签右缘。</li>
</ul>
<h2 seq="auto">侧栏导航</h2>
<p>按 <code>MENU[VIEW]</code> 渲染分组与菜单项，当前项高亮，含待办角标（如入驻审核 12、提现审核 3）。点击项切页；菜单 = 该层作用域投影。</p>
<h2 seq="auto">对象视角工具栏</h2>
<p>管理员切到代理商 / 服务商 / 用户视角时，页头下方出现「搜索」工具栏：按 ID / 用户名 / 昵称 / 手机号 / 区域模糊检索并下拉选择某一具体对象；选中后整页数据（首页 KPI、待办、列表）整体切换到该对象名下，面包屑与作用域标签同步；工具栏常显当前对象身份，可一键返回总台。搜索框支持输入即出下拉候选与「当前」标记。</p>
<h2 seq="auto">内容区与列表页</h2>
<p>列表页由统一引擎渲染：标题 + 副标题 + 作用域 chip + 分类筛选按钮（可点击快速筛选）+ 关键词搜索框（含「搜索」按钮与悬停清空叉）+ 导出按钮 + 数据表 + 总条数 + 「＋新建」主操作。操作列按页面类型渲染「查看 / 编辑 / 通过 / 驳回 / 处理」。</p>

<h1 seq="auto">菜单架构（四层）</h1>
<h2 seq="auto">总台（13 项 · 6 个顶级分组）</h2>
<table>
<thead><tr><th><p>分组</p></th><th><p>菜单项</p></th></tr></thead>
<tbody>
<tr><td><p>总览与治理</p></td><td><p>经营总览 / 区域管理 / 代理商管理 / 服务商管理（含入驻审核队列，角标 12）</p></td></tr>
<tr><td><p>运营监管</p></td><td><p>用户管理 / 模板审核 / 订单管理</p></td></tr>
<tr><td><p>财务中心</p></td><td><p>钱包总览 / 提现审核（角标 3）</p></td></tr>
<tr><td><p>评价与反馈中心</p></td><td><p>评价与反馈中心（角标 2）</p></td></tr>
<tr><td><p>消息中心</p></td><td><p>消息中心（角标 1）</p></td></tr>
<tr><td><p>系统</p></td><td><p>角色与权限 / 系统设置</p></td></tr>
</tbody>
</table>
<h2 seq="auto">代理商中心（8 项）</h2>
<p>辖区概览 / 辖区用户 / 辖区服务商 / 辖区订单 / 财务中心（结算与钱包）/ 评价与反馈中心 / 消息中心 / 角色与权限</p>
<h2 seq="auto">服务商中心（8 项）</h2>
<p>我的工作台 / 服务管理 / 订单处理 / 资质管理 / 财务中心（钱包与提现）/ 评价与反馈中心 / 消息中心 / 角色与权限</p>
<h2 seq="auto">用户中心（7 项）</h2>
<p>用户工作台 / 我的订单 / 我的服务商 / 优惠与权益 / 我的钱包 / 评价与反馈 / 消息中心</p>
